# 財務分析・決算棚卸し一覧

2026-09-15

- 財務分析ダッシュボードに「決算棚卸し一覧」を追加。店舗商品、製造食材・資材、卸倉庫・他店、食のブランド館の年度別保存データを読み取り専用で集計する。
- APIは既存棚卸しと同じNextAuthの許可アカウントで認証する。年度指定は2000〜2100、未指定は保存済みの最新年度。年度がない場合は未作成と表示し、他年度へフォールバックしない。明細は1000件ずつ全件取得する。
- 元画面の計算基準と円未満切り捨てを踏襲。店舗単価は販売価格から再算出し、卸倉庫の除外行は除く。税込・税別・実売単価ベースが混在するため金額基準を明記。入力中・未作成・要確認を含む合計は暫定と表示する。
- 食のブランド館は現在の編集用ワークブックの合計シートと各明細名を照合。現行は道の駅食材在庫・道の駅資材在庫・共通資材の3区分。合計シート自体を重複加算しない。数式エラー・対応不明は確認事項として表示し、その金額を確定値扱いしない。
- Excelは画面と同一時点の一覧と明細を出力。明細の単価・数量・文字を保持し、再配置に伴う参照切れを避けるため計算済み値を出力。一覧合計のみSUM式とキャッシュ値を付与する。
- 元画面へのリンクは選択年度のidを渡す。製造画面のid指定閲覧は既存仕様どおりマスター自動同期を行わない。元データ、棚卸し原本、会計帳簿、KPIの計算は変更しない。
- 検証: `node scripts/test-finance-closing-inventory.cjs`、既存の集計回帰テスト、対象ESLint、secret/RLS検査。全体tscは既存箇所のエラーあり、新規モジュール・変更画面の型エラーなし。ローカル認証URLを指定してビルド検証。
- `node scripts/check-finance-closing-inventory.cjs [baseURL]` は読み取り専用。2026年度8区分の金額と各元APIを突合し、未認証・別アカウント・不正年度・未作成年度・PC/スマートフォン・Excel実ダウンロードを検証。検証時合算53,482,414円、Excel9シート。
- 共有同期は既存共有リポジトリの未コミット変更で停止。GitHub mainの新規cloneから作業し、既存変更は保持。

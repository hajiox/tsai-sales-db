# TSA 裏ラベルチェック運用ノート

## 2026-08-25 TSA移植

- DocScannerの裏ラベルチェックを、社外からも利用できる認証付きTSA画面へ移植した。
- PCサイドバーは「バックアップ管理」の直下、スマホトップは「裏ラベル簡易チェック」から開く。
- 履歴: `/system/label-check`
- メインの簡易チェック: `/system/label-check/check?mode=simple`
- 通常チェック: `/system/label-check/check?mode=normal`
- 簡易チェックは画像1枚を端末内で縮小後、商品名・賞味期限・製造日だけを軽量モデルでOCRする。レシピ照合を行わないため、通常チェックより速い。
- 通常チェックは最大4画像、原材料OCR、TSAレシピ照合、手動レシピ選択、任意のレシピ学習を維持する。
- 履歴と判定情報はSupabaseの `label_checks`、画像参照は `label_check_images` に保存する。画像本体は非公開Storage `label-check-images` に置き、認証API経由でのみ表示する。
- DocScanner SQLiteから履歴169件、画像146件を移行した。通常124件、簡易45件、OK 141件、NG 25件、その他3件で移行前後が一致し、画像欠損は0件だった。
- 旧DocScannerのSQLiteと画面は直ちに削除せず、移植直後の復旧用として保持する。今後の新規チェックと履歴参照はTSAを正本とする。

## 検証

- 本番相当ビルド189ルート成功、対象Lintエラー0。
- 未認証APIは401、未認証画面はログインへ307転送。
- PC 1440px、スマホ390pxで横はみ出しなし。履歴画像の署名URL配信はHTTP 200。
- 簡易OCRは旧履歴の画像3枚で賞味期限が3/3一致し、応答は約1.4〜2.2秒だった。
- 保存APIは初回201、同一 `request_id` の再送は200かつ `duplicate: true`。試験データは削除済み。

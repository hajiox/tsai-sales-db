# TSA 財務分析 取込メモ

## 2026-07-31 複数ファイル取込

- 総勘定元帳と合計残高試算表で、複数ファイルの選択・ドラッグ&ドロップに対応
- ファイルごとに対象月と文字コードを自動判定し、対象月は個別修正可能
- 同じ対象月の複数ファイルは、月次データの上書き事故を防ぐため取込前に停止
- DB負荷と同月更新の競合を避けるため、画面上は一括操作、API処理はファイル単位で順次実行
- ファイルごとに待機・取込中・完了・失敗と処理件数を表示
- 一部失敗時は成功分を保持し、失敗ファイルだけ再実行可能
- 元帳取込後の財務集計更新は、全ファイル処理後に1回だけ実行
- 損益分析ビューアー内の旧単一ファイル取込は廃止し、複数対応画面へ統一
- 試算表の対象月自動判定は、決算期開始日ではなくファイル内の最新年月を採用

## 確認

- 2026年3月・4月・5月の実ファイルで、元帳・試算表とも対象月の自動判定を確認
- ダミー2ファイルで複数選択、一覧表示、個別対象月、プレビュー、エラー表示を確認
- PC幅・スマホ幅で横はみ出しなし

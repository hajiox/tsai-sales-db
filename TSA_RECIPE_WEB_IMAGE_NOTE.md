# TSA Recipe Web Image Note

## 2026-08-13

- レシピ詳細の「商品ポイント & Web商品説明」に、既存の商品写真とは分離した「Web商品画像」を追加。
- JPEG / PNG / WebP の手動アップロード、複数選択、ドラッグ＆ドロップ、削除、ドラッグ並び替えに対応。
- PCから登録する「ポートレート画像」を追加。レシピごとに枚数制限なしで、複数選択、まとめてドラッグ＆ドロップ、個別削除、ドラッグ並び替えに対応。
- 250KB超の画像はブラウザ側でWebPへ自動縮小し、APIとDBでも250KB上限を検証。
- `recipe_web_images` テーブルとVercel Blob保存APIを追加。取得元ページ・元画像URL・容量・表示順・画像用途を保持。ポートレートの枚数制限は設けない。
- 楽天市場の現行68商品とBASEの現行24商品を調査し、JAN一致または数量を含む強い商品名一致だけを自動照合。
- 楽天・BASE初期登録: ネット専用53レシピ / 楽天453枚、自社単品6レシピ / BASE37枚。
- 共有フォルダ `\\tshdd\disk\NEW\★商品パッケージ★` から、EC画像が空のレシピだけを明示対応表で補完。ネット専用46レシピ / 462枚、自社25レシピ / 261枚。
- 初期登録合計: 130レシピ / 1,213枚。曖昧な名前の自動マッチは行わず、楽天・BASE・共有フォルダの取得元をDBに保持。
- 楽天の別ドメイン重複画像183枚と空白画像1枚を除去。重複キーは商品ごとの画像パスで判定。

### Verification

- `npm run build`: success
- Production: `https://v0-tsa-19.vercel.app`
- 本番画面で「Web商品画像」、画像一覧、追加ボタン、ドロップ領域を確認。
- 本番画面で「ポートレート画像」の3:4プレビューとPC用ドロップ領域を確認。コンソール警告・エラーなし。
- 当初の1枚制限は撤廃。本番APIで3枚の同時保持、逆順への並び替え、一括削除を検証。既存Web商品画像12枚は不変で、テスト画像はすべて削除済み。
- サンプル `【ネット】いかにんじんふりかけ2個セット`: 重複整理後7枚、ブラウザコンソール警告・エラーなし。
- DB検証: 1,213枚 / 130レシピ、250KB超0件、商品内の取得元重複0件、共有フォルダ取込失敗0件。
- 取込・再検査スクリプトは `scripts/recipe-web-images/` に集約。

### Remaining

- 旧商品、終売ページ、セット数量違い、現在ECページが存在しない商品は誤登録防止のため自動投入していない。
- 再調査時は `scripts/recipe-web-images/backfill.mjs` で照合レポートを作り、確実な一致だけを登録する。
- 共有フォルダの `【ネット】喜多方ラーメン麺のみ6食（細麺）` は対応するレシピがマスターに存在しないため未登録。

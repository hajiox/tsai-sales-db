# 棚卸し集計の円未満切り捨て

2026-09-15

- ユーザーの訂正により対象は棚卸し集計のみ。財務分析・決算書・KPI・商品マスター・単価算出・数量算出は変更しない。
- 店舗、製造、卸倉庫、他店棚卸し、食のブランド館の明細棚卸金額を円未満切り捨てし、表示明細と合計を一致させる。CSV/Excel/印刷も同じ値を使用する。
- 共通処理 `lib/inventory-total.ts` は小数部分を切り捨て、浮動小数点の整数境界誤差だけを補正する。単価・数量・税率の保存値は保持。
- 食のブランド館は金額ヘッダーの列だけを計算対象とし、通常GETでも再計算して既存年度に適用する。DB取込原本・旧履歴は変更しない。通常保存時のみ編集用データの計算結果を更新する。
- 食のブランド館Excel出力は金額式をROUNDDOWN(...,0)とし、Excel側の再計算と再取込でも切り捨てを維持。単価・個数・文字・参照切れはそのまま。
- 検証: 数量1.5・単価1.93の保持、金額切り捨て、合計一致、負数・ゼロ・整数境界、Excel式再取込、原本保持、卸画面/印刷計算のテストを実施。原本471セル・118計算式の従来処理テストも維持。

`node scripts/test-inventory-totals.cjs`
- 対象ESLint、集計回帰テスト、原本118式テスト、secret scan/RLS、本番用ビルド189ページ生成に合格。共有同期は既存共有cloneの未コミット差分により停止し、新規GitHub cloneで作業。

# TSA Codex Bridge

The bridge runs on the office Windows PC and claims allow-listed TSA jobs over outbound HTTPS. Four role-specific workers share the durable queue: one interactive Chrome worker, two browserless AI-generation workers, and one browserless analysis worker. All stream progress back to TSA and upload execution artifacts.

## Runtime

- Browserless jobs continue before Windows login through three S4U tasks: `TSA Codex Bridge (AI 1)`, `TSA Codex Bridge (AI 2)`, and `TSA Codex Bridge (Analysis)`.
- Chrome-dependent sales, advertising, settlement, price, product-name, catchcopy, and product-content jobs wait in the durable queue until the Windows user is signed in and the interactive worker is online.
- The two AI workers share only generation and summarization jobs. The analysis worker claims only WEB sales analysis. Atomic database claims prevent duplicate execution when both AI workers are idle.
- Codex desktop and the signed-in EC Chrome session are required only for Chrome control.
- The shared archive path must be reachable.

## Unified monitor

Bridge `1.9.2` and later use one persistent `Codex Bridge 統合モニター` window per signed-in Windows session. It reads strict UTF-8 state files from `%LOCALAPPDATA%\Codex Bridge Monitor\states` and displays TSA, TSG, and DocScanner together. Closing the monitor never stops a worker or Codex process; the next interactive Bridge event reopens the same monitor. The three pre-login workers publish separate state rows but never open desktop windows.

Each future TSG or DocScanner Bridge must publish one atomic `*.json` file that conforms to `bridge-monitor-state.schema.json`, using `system` values `tsg` or `docscanner`. Until such a Bridge exists, the monitor reports that system as `未導入` instead of inventing activity. A stale heartbeat is shown as `応答停止` when the worker PID still exists and `オフライン` when it does not.

## Install

Run `install-bridge.ps1` with the same secret stored in Vercel as `TSA_CODEX_BRIDGE_TOKEN`. The installer copies runtime files to `%LOCALAPPDATA%\TSA Codex Bridge`, keeps the current-user startup entry for the interactive worker, and registers the three browserless workers as `AtStartup` S4U tasks. The first registration uses one Windows administrator confirmation for all three tasks, but stores no Windows password. Later Bridge updates reuse the same tasks.

Every worker has a different worker ID, runtime directory, lock, state file, job directory, log, and monitor key. The database claim function filters on each exact capability list, so browserless workers cannot claim Chrome or shared-folder collection jobs. All four workers share one maintenance lock, and the installer waits until every active worker is idle before replacing files.

Each production job starts with a fresh `codex exec` session and never resumes the TSA development chat or another long-lived task. The rollout is retained only so the Chrome plugin can observe turn completion and release claimed tabs; it is not reused as operational context. Signed-in EC jobs first list and claim the user's already-open matching Chrome tab. If every matching tab is owned by another Codex browser session, the job creates at most one temporary tab per target EC in the same selected Chrome profile and reuses that profile's login state. It never opens another browser, window, incognito session, or temporary profile, and never closes an operator-owned tab. Login, MFA, CAPTCHA, account selection, or a real permission denial stops only after the actual official authentication screen or prompt is visible.

Every TSA button that invokes Codex must route to a dedicated Skill. Do not send a task/thread ID or development-chat history to the worker. A new recurring workflow is not production-ready until its Skill and compact structured input are defined.

`skill-contract.json` is the runtime source of truth for the task-to-Skill mapping. The worker rejects job parameters containing Chat, thread, conversation, rollout, transcript, or session context, verifies the required installed Skill immediately before every Codex launch, and starts Codex only through the guarded `codex exec` path. The installer derives its task and Skill lists from the same contract and installs every required Skill, including `update-aizu-ec-prices`; there is no session-ID setting.

EC product-name jobs use the dedicated `update-aizu-ec-product-names` Skill. TSA stores one common product name and sends that exact string to all seven marketplaces. Jobs process one marketplace at a time, mutate only the exact product-name field, preserve successful sites when another site fails, and expose an unfinished-only retry. Immediate changes create one TSG report; jobs released from the shared reservation queue create one aggregate report after the batch completes.

EC product-name candidate generation uses the dedicated `generate-aizu-ec-product-names` Skill and `gpt-5.6-sol` at medium reasoning. The job receives only the compact saved recipe snapshot, never resumes a development chat, never opens an EC site, and saves one versioned common candidate without changing the recipe or external listings. The candidate must satisfy the combined rules of all seven marketplaces and the strict shared 75-character limit. It requires explicit adoption and recipe save before the existing EC name-update workflow can run. The result dialog can return to editing without adoption, reopen the latest result, or regenerate it as a separate immutable history entry.

The production screen is `/web-sales/automation`. It supports both product-sales aggregation and advertising-cost collection. Use `PC接続テスト` before the first run.

Advertising jobs reuse TSA's existing import, auto-match, and cost-update routes. Google Ads uses the configured Google Ads API; Meta, Rakuten RPP, Yahoo item reach, and Amazon Sponsored Products use the signed-in Chrome session and the `tsa-ad-cost-csv` Skill. A report with positive-cost unmapped rows finishes as `needs_review` instead of updating advertising costs.

Monthly WEB sales and EC expense analysis uses `gpt-5.6-sol` through the official Codex CLI and the `tsa-web-sales-analysis` Skill. TSA builds a compact deterministic packet, embeds it once in the isolated task, validates structured output, and saves every run as an immutable numbered version in `web_sales_ai_analyses`.

This TSA Bridge is an authenticated outbound queue worker around the official `codex exec`. It is not a third-party OpenAI-compatible localhost proxy and is not exposed to the internet. Vercel queues jobs; the allow-listed office PC claims and completes them.

## Runtime version tracking

The Bridge checks the Codex Desktop runtime while idle and immediately before every Codex job. On Windows it accepts a CLI only when the matching `codex-code-mode-host.exe` exists, and automatically switches to a newer versioned Codex directory. The worker heartbeat and every terminal job result record the Bridge version, Codex version, executable update time, runtime check time, and readiness. This applies to product-sales CSV, advertising-cost, EC settlement, and monthly analysis jobs.

The TSA automation screen shows both versions and warns when the installed Bridge does not match the required production version. A Bridge source update still requires reinstalling or copying the new Bridge file and restarting the worker; Codex Desktop updates do not require a Bridge restart.

## Operation note

- 2026-09-04: Bridge 1.9.62 adds a Qoo10-only `ec_product_register` workflow for a price-verified missing recipe product. The administrator confirms one immutable payload hash covering account, title, JAN/seller code, price, description, ordered images, and the fixed sibling reference. A durable intent serializes enqueue, permits exactly one external submit, blocks automatic resend after an uncertain response, and stores an immutable Qoo10 identity only after full post-save verification. The recipe screen exposes registration only after a Qoo10 `not_found` result and displays progress, review state, and the resulting product number through the unified monitor contract.
- 2026-09-04: Bridge 1.9.63 makes the Qoo10 registration plan and result schemas valid Codex structured-output schemas by requiring an explicit JSON type on every property. Contract coverage now rejects future properties that omit `type` before a production job can claim them.
- 2026-09-04: Bridge 1.9.64 prevents duplicate creation when Qoo10 already has the same product under stale identifiers. A proven single listing may be updated in place only when account, normalized title, flavor, storage method, sale format, and pack count match and replacement JAN/seller code have no collision; the same one-submit guard and complete post-save verification apply.
- 2026-09-03: The Qoo10 price playbook now treats an old-QSM save timeout as indeterminate instead of immediately retrying or reloading. It recovers and accepts the product-specific JavaScript confirmation dialog, waits for the legacy save handler, verifies the server value, and only then uses one alternate activation path. This prevents a premature reload from cancelling a valid save while preserving the two-attempt limit.
- 2026-09-03: Bridge 1.9.61 accepts an evidence-backed marketplace `not_found` plan even when the model echoes the locked standard baseline in its otherwise empty price fields, preserves Amazon's actually visible old price when a submitted target is still inside the stated reflection window, and uses Yahoo's store-account-specific `pro.aizubrandhall` management root instead of the invalid host root. Runtime and server validators cover both accepted states without advancing a pending price baseline, and `submitted_pending` is excluded from unfinished-only retries to prevent duplicate submissions during the official reflection window.
- 2026-09-03: EC price updates no longer use Chrome history to recover marketplace routes. Mercari Shops now opens the current official `/seller/shops/` management root, treats only a redirect to `/signin/seller` as signed out, and blocks just Mercari without requesting history permission when the route is unavailable. The EC price safety and Skill-contract tests passed; the current office worker consumes the matching installed Skill without interrupting active jobs.
- 2026-09-03: Bridge 1.9.60 treats a saved common EC name as untrusted when it conflicts with the recipe, JAN-linked evidence, or learned marketplace titles on flavor, form, or set count. The worker may use only a non-conflicting exact identity and otherwise blocks that marketplace instead of selecting a similarly named product.
- 2026-09-03: Bridge 1.9.59 registers the verified `tsukemen.aizubrandhall-lp2.com` production source and distinguishes TSA manufacturing fill targets from EC declared net-content weights. Exact identifiers or exact mapped-title evidence may proceed despite that numeric-only difference, while flavor, form, set count, storage, and ambiguous-product conflicts still fail closed.
- 2026-09-03: EC price retries now recheck every requested site when the old plan failed server validation, while validated plans continue to retry only unfinished sites. This prevents a false `updated` result from permanently hiding a marketplace after a rejected plan.
- 2026-09-02: Bridge 1.9.57 expands TSA from two workers to four bounded role workers: one signed-in Chrome worker, two pre-login AI-generation workers, and one pre-login WEB-analysis worker. Each has an isolated runtime, lock, state, job root, worker ID, and unified-monitor row while sharing the atomic durable queue. The installer migrates and unregisters the former single pre-login task only after every active worker acknowledges maintenance and is idle. The TSA automation screen reports all four workers separately.
- 2026-09-03: Bridge 1.9.58 stops cleanly when the server requires a newer runtime, quarantines the retired single headless worker without repeated 409 logging or a stale monitor row, carries one verified pre-change standard price through every sequential EC-price phase, and makes idle versus last-finished monitor output explicit. Qoo10 manual mapping now replaces stale job result text with the finalized import result.
- 2026-09-02: Bridge 1.9.56 corrects the SNS `developer` voice to the owner's actual public voice. First-person references use `私`, sentences are polite `です・ます` Japanese, and the Skills avoid commands, lecturing, confrontation, superiority, strong assertions, and pressuring phrases. TSA rejects newly generated developer posts containing `俺`/`僕` or representative coercive wording while retaining read compatibility for immutable older histories.
- 2026-09-02: Bridge 1.9.55 removes SNS ImageGen path discovery from Codex commands. Each fresh session generates exactly one image per requested platform in order; the deterministic worker enumerates only that session's image directory, requires the exact image count, maps files by creation order, validates any supplied path, and uploads them. The Skill now forbids `Get-ChildItem` and `Copy-Item`, eliminating command-shape drift while tightening the safety boundary. TSA validates those authenticated image artifacts instead of requiring an AI-supplied local absolute path.
- 2026-09-02: Bridge 1.9.54 attempted to admit one exact read-only ImageGen listing pipeline, but a legitimate alternative PowerShell shape still failed closed. It was superseded by the deterministic 1.9.55 path resolver.
- 2026-09-02: Bridge 1.9.53 adds an immutable SNS writing-tone contract. Each fresh `recipe_sns_generate` job receives exactly one of `official`, `staff`, or `developer`; the dedicated Skill defines the voice, the structured output must echo it, and TSA rejects mixed or mismatched targeted regeneration. Existing histories default to `official` without a schema migration.
- 2026-09-02: Bridge 1.9.52 replaces Qoo10 product-sales browser collection with the existing DocScanner official Shipping API sync. The worker checks statuses 1 through 5, reconciles the API order count with the saved official rows, creates and validates the CSV locally, and never reuses the former four-screen zero evidence. API errors, disabled sync, and count mismatches fail closed instead of becoming zero sales. The same QAPI v3 status coverage is used by TSA's direct connector.
- 2026-09-01: Bridge 1.9.51 fixes Qoo10 zero-sales evidence after QSM's upper current-order Summary was mistaken for the requested-period detail result. Zero verification now opens the lower delivery detail panel, fixes the full-day time range, and accepts only `#GoodsGrid` with the matching D1-D4 state, empty marker, and zero page total. Evidence schema 2 records the grid, status code, and time bounds so current orders outside the period cannot block or falsely validate a monthly result.
- 2026-09-01: Monthly finance status now treats the persisted import as authoritative instead of letting a later login/download retry overwrite the visible state. Advertising and EC-control cards prefer completed imports, and an official partial EC import is shown as `一部確定`; queued/running retries still remain visible while active, and failed retries remain in history. Unfinished-only execution skips channels whose official data is already stored. Runtime tests cover completed-import precedence, official partial evidence, estimates, active retries, and non-finance jobs.
- 2026-09-01: Bridge 1.9.50 no longer accepts a header-only Qoo10 sales CSV as period-confirmed evidence. A zero result now requires strict matching evidence for 会津ブランド館, 注文日, the requested period, and all four delivery states, with SHA-256-verified PNGs; archived zero CSVs without that evidence are not reused. Verified zero results are displayed as `公式画面で対象期間0件を確認済み` so missing acquisition and genuine zero sales cannot be confused. The 2026-08 QSM order-date, payment-date, and purchaser-payment searches were independently confirmed as zero.
- 2026-09-01: Bridge 1.9.49 makes EC evidence precedence provenance-aware: an official import replaces a ratio-only estimate even when its coverage remains partial, while a weaker retry cannot overwrite an official partial result. Yahoo recovery rejects HTML disguised as CSV, stages the official daily sales report with billing and receipt details, classifies the billed seller portion of `特典の一部利用料` without treating the receipt as a deduction, and displays full-month partial evidence as `一部確定`. Verified with the import-policy, download-recovery, Skill-contract, retry, ESLint, and production-build suites; Rakuten still requires a current BillPay session and published August statement.
- 2026-09-01: Bridge 1.9.38 hardens signed-in EC CSV downloads after an Amazon job overlapped another Codex Chrome session. Product-sales jobs now select one Chrome binding for the full run, stop instead of switching extension instances after a detach, attach rejection handling before every download click, and never retry a dismissed Browser approval in the same job. Waiting results distinguish Codex Browser download approval from Amazon login and Chrome site settings, and the automation screen shows the structured reason and the correct next action.
- 2026-09-01: Bridge 1.9.39 keeps Amazon report downloads waiting for up to five minutes because report generation can legitimately exceed two minutes. The worker still clicks once, stops immediately on a Browser approval failure, and treats only a full five-minute no-event result as a technical failure.
- 2026-09-01: Bridge 1.9.40 restores bounded recovery for a seller tab left owned by an earlier browser-operation session. The worker preserves the operator tab, opens at most one temporary tab in the same selected signed-in Chrome binding, never switches browser bindings, and closes only its temporary tab.
- 2026-09-01: Bridge 1.9.41 pins Amazon temporary-tab recovery to the confirmed `/business-reports` route. It no longer mistakes Seller Central's generic `/amazonsell/business` marketing redirect for the authenticated report destination.
- 2026-09-01: Bridge 1.9.42 recovers Amazon Business Reports CSV downloads when Codex Browser approval closes. The local worker revalidates the exact child-ASIN report URL, 会津ブランド館 account, requested dates, and named CSV button through Windows UI Automation before one bounded invocation; browser timeouts continue with a local folder watch instead of a second click.
- 2026-09-01: Bridge 1.9.43 recovers verified advertising and EC-settlement reports from Downloads, stages originals in the job folder, and leaves final network archiving and TSA import to the Bridge. Existing period-matched files can be reused without repeating a blocked browser download.
- 2026-09-01: Bridge 1.9.44 requires channel and requested-period evidence before reusing an EC settlement file, and releases claimed operator tabs when each fresh Bridge session ends so later jobs can reuse the same signed-in tab.
- 2026-09-01: Bridge 1.9.45 revalidates archived EC originals without trusting the Bridge-added period prefix, preventing stale or cross-channel files from being reused. Amazon and TikTok settlement jobs may recover one already-verified official download through the claimed tab's same-origin CDP runtime when Chrome alone blocks the save; authentication and real permission prompts still stop immediately.
- 2026-09-01: Bridge 1.9.46 enters Amazon Ads through the signed-in Seller Central Marketing > Ads Console route instead of the public Amazon Ads sign-in page. Google Ads automation counts unique unknown ad groups, allocates recognized BASE-wide Shopping spend by BASE sales composition, excludes physical 食ブラ visit/search spend from WEB sales advertising, and replaces the full month's Google allocation only after validation.
- 2026-09-01: Bridge 1.9.47 tries every matching signed-in Seller Central or Amazon Ads tab before opening a temporary tab, so one stale or busy tab no longer blocks Amazon advertising import.
- 2026-08-31: Bridge 1.9.36 makes SNS publication stops deterministic. Generated SNS variants are JPEG instead of WebP for Instagram compatibility. A non-interactive `codex exec` no longer treats TSA's stored scope record as user-authored action-time confirmation, retries dismissed Browser upload approval, or reports that condition as login failure; it preserves unposted platforms and returns an explicit interactive-Codex wait. Meta Business Suite's OS-only Story file picker is handled by the same fail-closed boundary. The approval policy passed 20 repeated runs, the SNS/Bridge contract suites passed, focused ESLint passed, and the production build passed. Unattended browser publication still requires an official platform API or an interactive action-time confirmation.
- 2026-08-29: Bridge 1.9.24 resolves and validates the machine-specific monitor placement during installation and stores only its compact Base64 coordinates in the local Bridge configuration. Scheduled-task startup therefore passes display 3 placement without reopening or racing the separate monitor configuration file; runtime file discovery remains a fallback.
- 2026-08-29: Bridge 1.9.23 validates the saved absolute monitor placement in Node and passes it directly to both the hidden launcher and visible monitor as compact Base64 JSON. Scheduled-task startup no longer depends on PowerShell discovering the local display configuration file during console initialization; the file remains the persistent source of truth.
- 2026-08-29: Bridge 1.9.22 makes the monitor verify its own configured position for three consecutive refreshes after startup and reapply it when Windows overwrites the first console placement. The launcher records bounded startup diagnostics, while the existing single monitor PID is reused instead of flashing replacement PowerShell windows.
- 2026-08-29: Bridge 1.9.21 stabilizes the configured monitor position for eight seconds after acknowledgement. Windows may apply its default cascading console position after the first successful move; the launcher now reapplies placement to the same acknowledged window until console initialization is complete.
- 2026-08-29: Bridge 1.9.20 closes an installer race with the interactive Scheduled Task watchdog. Updates now stop the task itself before replacing runtime files and start it only after every Bridge and monitor file is installed, preventing a new worker from loading a mixed old/new monitor bundle.
- 2026-08-29: Bridge 1.9.19 supports validated absolute monitor coordinates in the local machine configuration. The office PC stores both display identity and the resolved display-3 work-area coordinates, so a background Scheduled Task can place the visible monitor even when Windows returns no screens to that background process.
- 2026-08-29: Bridge 1.9.18 makes the visible-console launcher wait for the new monitor acknowledgement and then place that exact existing window. This covers the scheduled-task/conhost startup path where placement inside the monitor process is unavailable, without creating a second monitor.
- 2026-08-29: Bridge 1.9.17 makes the monitor resolve its bundled placement helper and persisted machine configuration even when the visible-console launcher does not bind the optional placement arguments. The fallback is runtime-tested without either argument and keeps the configured display after worker or Windows restarts.
- 2026-08-29: Bridge 1.9.16 retries the persisted monitor placement after startup when Windows exposes the display/configuration slightly after the console process begins. The already-running monitor applies the recovered placement itself, updates its acknowledgement, and does not open another console.
- 2026-08-29: Bridge 1.9.15 fixes unified-monitor flicker caused by a live Windows PowerShell monitor being misclassified through locale-sensitive `tasklist` CSV parsing. PID liveness now uses the operating-system process probe, requires three consecutive failed checks before replacing an existing acknowledgement, and lets the persistent monitor restore a missing acknowledgement without opening another console window.
- 2026-08-29: The unified Bridge monitor supports an optional persistent `%LOCALAPPDATA%\Codex Bridge Monitor\monitor.config.json`. When present, both a newly opened monitor and an already running monitor are moved to the configured display, offset, and size. The device name is preferred, the one-based display number is the fallback, and an unavailable target safely falls back to the primary display. Installer updates preserve this local machine-specific file.
- 2026-08-27: Bridge 1.9.6 adds `docscanner_fax_summary` for incoming FAX posts. DocScanner first posts the existing PDF-derived page images to TSG, then queues only a compact source ID plus verified local image metadata. A fresh ephemeral GPT-5.6 Luna / low session reads only the dedicated `summarize-docscanner-fax` Skill and copied page images; Chat history, prior sessions, repositories, web access, browser control, plugins, and arbitrary files are excluded. The worker verifies the allow-listed root, real paths, byte sizes, and SHA-256 values before execution, validates structured Japanese output, and PATCHes the same TSG post without replacing attachments. Idempotency prevents duplicate active jobs, failures keep the FAX images visible, and the unified monitor reports sanitized DocScanner progress without FAX text. The installer writes every Windows PowerShell payload with a UTF-8 BOM so Japanese scripts parse before login; a Windows PowerShell 5.1 syntax test covers the registration script. The production migration, contract tests, pre-login coverage, unified-monitor coverage, focused ESLint, and environment-backed production build passed; no synthetic FAX post was created.
- 2026-08-27: Bridge 1.9.5 updates ingredient-label generation to rules `2026-08-27.2`. TSA now locks the top-level weight-rank-1 origin target in the hashed source packet, permits an origin/manufacture annotation only for that item, and rejects a missing, mismatched, repeated, or rank-2-or-lower origin at import. The dedicated Skill uses the current April 2026 list of 9 mandatory and 20 recommended allergens, returns every supported present item in one collective review draft, and the server rejects any mismatch between the canonical allergen array and statement. Unresolved ranking, origin, composition, or allergen evidence remains adoption-blocked and human review remains mandatory.
- 2026-08-27: Bridge 1.9.4 makes the signed-in interactive worker durable through a limited current-user Windows logon task with an unlimited runtime, single-instance policy, and automatic supervisor restart after an unexpected process exit; HKCU Run remains only as a registration fallback. The unified monitor opens state files with strict UTF-8 plus read/write/delete sharing, while the Bridge retries only bounded Windows sharing violations during atomic replacement instead of intermittently logging `EPERM` and leaving stale progress. Runtime coverage deliberately holds the monitor read handle open while repeatedly replacing Japanese state JSON.
- 2026-08-27: The ingredient-label generation screen now treats the durable job row as the authoritative progress source. A transient API or deployment interruption no longer terminates polling and leaves an old running percentage on screen; polling retries, tab/window focus triggers an immediate resync, and a completed result is restored even when the page did not already hold a label. Verified against the production job that completed at 100% while the old client remained at 16%.
- 2026-08-27: Bridge 1.9.2 adds the dedicated product-content workflow. The combined 500-character counter and GPT-5.6 Sol adjustment action now sit immediately above the actual product-points and Web-description fields. A saved recipe can be sent to one EC or all seven, one site per isolated Skill session, while completed sites survive later failures and unfinished-only retry remains available. Amazon receives separate bullet and description fields; every other site receives points, a blank line, then description in one field. Rakuten and Yahoo receive the `■` variant, while Amazon, Mercari Shops, BASE, Qoo10, and TikTok receive the `✅️` variant. Both generation and seller updates use dedicated Skills, compact versioned packets, exact snapshots, administrator authorization, schema validation, and no app Chat or prior session context.
- 2026-08-27: Bridge 1.9.1 changes EC product names and catchcopies from site-specific values to one authoritative common value. Product-name AI balances all seven marketplace rules and returns one candidate of at most 75 characters; TSA fans that exact string out to Amazon, Rakuten, Yahoo, Mercari Shops, BASE, Qoo10, and TikTok. Catchcopy AI returns one candidate of at most 30 characters and sends that exact string to the dedicated Rakuten and Yahoo fields. The recipe screen no longer lists current values per marketplace. Save routes, job snapshots, administrator authorization, Bridge validation, output schemas, and four dedicated Skills all reject divergent compatibility maps. Exact Bridge version enforcement prevents an older worker from claiming these jobs.
- 2026-08-27: Recipe EC images now have one positional contract shared by the screen and future EC image Bridge work. Gallery slot 0 is Amazon/BASE listing order 1 and shows a green number badge as their exclusive top image; slot 1 is listing order 1 with a blue badge and the top image for the other EC sites; slot 2 onward is shared by every EC with its displayed number as the listing order. Mercari-derived shipping/company images are stored separately as red `non_amazon` images and appended only to Rakuten, Yahoo, Mercari, Qoo10, and TikTok; Amazon excludes them. BASE uses separate yellow `base_only` shipping/company images because its regular-temperature delivery terms differ, and never receives the red set. The API returns deterministic per-site `ecImageSets`, so a future image Bridge must consume the locked set instead of reinterpreting badge colors. Dragging a Web product image into the portrait area creates a separate managed Blob with copy lineage, so deleting either image cannot break the other.
- 2026-08-27: Bridge 1.9.0 replaces per-job TSA monitor windows with one persistent unified monitor contract for TSA, TSG, and DocScanner. TSA interactive and pre-login workers publish atomic strict UTF-8 state snapshots to the common state directory; only the interactive worker opens or foregrounds the single mutex-protected console. The monitor shows progress, current step, elapsed time, ETA, last response, worker/Codex PIDs, operator waits, recent terminal history, malformed-state errors, stale heartbeats, and accurate `未導入` status for systems that do not yet have a Bridge. Runtime tests cover Windows PowerShell 5.1 Japanese parsing, all three systems, stale-state detection, restart from saved state, and proof that closing the monitor does not stop a worker.
- 2026-08-26: Bridge 1.8.47 fixes the visible monitor appearing with only its header. Windows PowerShell 5.1 interpreted the Bridge's BOM-less UTF-8 `monitor-state.json` as the system ANSI code page, so Japanese JSON parsing failed inside a silent retry loop. The monitor now reads strict UTF-8 through `File.ReadAllText`, immediately shows a loading state, surfaces persistent read retries instead of staying blank, and has a Windows PowerShell 5.1 runtime test using Japanese task, product, step, and summary text.
- 2026-08-26: Bridge 1.8.46 renders the fixed `会津ブランド館` creative label from Unicode code points so Windows PowerShell 5.1 cannot reinterpret the UTF-8 script literal. Production X targeted regeneration created a corrected 1600x900 creative with intact Japanese copy; URL comparison confirmed that only X changed while Instagram, IG Story, and Threads remained byte-for-byte referenced to the previous version.
- 2026-08-26: Bridge 1.8.45 upgrades recipe SNS generation to protocol 3. Each platform card can regenerate only itself in a fresh minimal-context Skill session; the server merges that one validated result into a new immutable history version while preserving the other three platform outputs. Creative rendering replaces the opaque black panel with a photo-led edge scrim, restrained brand line, and responsive Japanese typography. The dedicated Skill now asks ImageGen for real negative space and rejects dark template panels. Focused feature, Bridge/schema, target-merge, image-layout, renderer, ESLint, migration dry-run, and production targeted-regeneration checks passed; the Windows PowerShell label issue found by the first smoke is corrected in 1.8.46.
- 2026-08-26: Bridge 1.8.44 upgrades `recipe_sns_generate` to protocol 2 and the dedicated `generate-aizu-sns-assets` Skill. The recipe SNS tab now offers `通常リサイズ`, `クリエイティブ`, and `アレンジ`. Normal mode performs deterministic platform sizing; creative and arrange modes attach exactly one locked product image to a fresh minimal-context GPT-5.6 Sol session and use built-in ImageGen for four platform-specific assets. Creative Japanese copy is rendered deterministically after image generation. Every output returns through authenticated job artifacts and is revalidated before immutable Blob publication. No development Chat, prior task, repository, web search, browser session, or external posting is loaded or used. The command audit normalizes Codex's escaped Windows path representation, then permits only the fixed local ImageGen Skill read plus a bounded generated-image copy; arbitrary shell activity remains blocked.
- 2026-08-25: Bridge 1.8.41 centralizes all 11 task contracts in `skill-contract.json`: one deterministic connection test and ten dedicated-Skill Codex workflows. Every Codex process now passes through one runtime guard that requires the mapped Skill, prepends the fresh non-resumed/no-Chat contract, rejects Chat/thread/conversation/rollout/session fields in queued parameters, and rejects any CLI route other than new `codex exec`. The legacy session-ID installer/config path was removed. All ten Skills are versioned and installed from the Bridge package, including the previously local-only price-update Skill, and `test:bridge-skill-contract` audits the mapping, packaging, prompts, and launch path.
- 2026-08-25: Bridge 1.8.40 adds a second S4U/AtStartup worker that survives Windows Update restarts and runs before interactive login. It is fail-closed to five browserless jobs, uses a separate lock/state/log/job directory and worker ID, exposes no Chrome capability or desktop monitor, and shares safe installer maintenance with the interactive worker. Chrome-dependent EC jobs remain queued until login. The automation screen now reports browser operation and pre-login processing separately. Initial elevated registration uses an encoded handoff and records a local registration status without credentials.
- 2026-08-25: Bridge 1.8.39 adds `recipe_sns_generate`. The recipe detail screen is split into Recipe, EC information, and SNS tabs. TSA deterministically selects a random portrait image (or the first Web product image), crops separate 16:9, 1:1, 9:16, and 4:3 WebP assets with Sharp, and stores immutable versions. A fresh isolated GPT-5.6 Sol / medium session reads only the dedicated `generate-aizu-sns-posts` Skill and compact saved EC/allow-listed LP snapshot to create X, Instagram, Instagram Story, and Threads copy. It cannot browse, post, or modify external data.
- 2026-08-25: Bridge 1.8.38 adds site-specific catchcopy management for Rakuten and Yahoo only. Yahoo uses the official `headline` field (30 full-width characters, HTML prohibited, search-indexed); Rakuten uses the dedicated RMS catchcopy field. The recipe screen stores separate values, generates both with GPT-5.6 Sol, supports individual or black `全EC` execution, reservations, progress/ETA, immutable generation and change history, and unfinished-only retry. Bridge updates one site at a time and preserves completed work when the other site is blocked. Amazon, Mercari Shops, BASE, Qoo10, and TikTok are excluded because this workflow has no dedicated catchcopy target for them.
- 2026-08-25: Recipe price and product-name sync controls place the black `全EC` action directly after the seven marketplace buttons. Immediate and reservation behavior remains selected by the mode control above the row.
- 2026-08-25: Bridge 1.8.37 switches dedicated EC product-name candidate generation from GPT-5.6 Terra to GPT-5.6 Sol at medium reasoning. The worker capability, job guard, UI labels, generation audit model, and database claim condition now use the same model identifier.
- 2026-08-24: Bridge 1.8.33 adds protocol 1 for `ec_product_name_update`. The recipe screen supports individual EC execution, all-EC execution, shared batch reservations, progress/ETA, per-site results, unfinished-only retry, and separate product-name history. The Bridge uses a dedicated compact Skill, verifies the recipe snapshot immediately before each write, and prohibits price, stock, shipping, tax, description, image, category, variation, advertising, and bulk mutations.
- 2026-08-23: Bridge 1.8.28 requires a monitor-side acknowledgement before logging the visible PowerShell monitor as started. The worker verifies the monitor PID and foreground request. Both initial launch and retry use the visible-window launcher without Node's detached mode, which was reproduced to suppress the monitor on the office PC. Recipe price reports to TSG are released only after the later EC/LP Bridge campaign completes; the hourly durable outbox remains the retry path.
- 2026-08-23: Bridge 1.8.29 starts the visible monitor through `conhost.exe`, uses one named mutex per job, disables console Quick Edit so a click cannot pause monitoring or auto-close, and appends progress when a step changes instead of repeatedly clearing the screen. It also accepts a validated read-only EC plan as final verification when the exact product's server-saved price already equals the locked target, avoiding a second Codex write session while preserving the same validation and audit trail.
- 2026-08-23: Bridge 1.8.30 briefly raises the single monitor window to the foreground before returning it to normal z-order, so Windows cannot leave a successfully created monitor hidden behind Chrome when direct focus activation is denied.
- 2026-08-23: Bridge 1.8.31 locks the verified 50-serving Oーション product identifiers for all seven EC targets and accepts the recipe snapshot plus the exact verified-product record as storage evidence when an optional seller field is blank but no contradictory value is shown.
- 2026-08-23: Bridge 1.8.32 closes a stale acknowledged monitor before the next job and force-closes any monitor still running 40 seconds after a terminal job state. The monitor keeps its own normal 30-second close path, so a paused or blocked Windows console cannot accumulate blank PowerShell windows.
- 2026-08-23: The server-locked four-serving Oーション registry now includes the verified Mercari Shops product ID and SKU found during the completed price campaign, preventing future false `not_found` results.
- 2026-08-22: Bridge 1.8.23 runs every EC and the registered product LP as independent sequential work items. A blocked item no longer invalidates successful writes or prevents later items from running; the retry action queues only blocked, submitted-pending, missing, or unfinished-LP items. The registered LP URL is verified directly, while a valid Vercel-generated deployment URL remains deployment evidence and is not required to equal the alias. The generic two-minute unchanged-step warning was removed because legitimate seller and deployment operations routinely exceed two minutes.
- 2026-08-22: Bridge 1.8.24 accepts an LP-only retry without requiring an EC reference price. The LP still requires an allow-listed GitHub source, exact product evidence, occurrence-level target prices, deployment evidence, and verification on the registered public URL.
- 2026-08-22: Bridge 1.8.25 resolves an LP plan's relative `source_file` strictly beneath its verified fresh-clone project root. Absolute paths remain allowed only when they are inside the same project root.
- 2026-08-22: Bridge 1.8.26 treats an LP as idempotently complete when every verified fresh-clone occurrence already equals its target and the registered public URL exposes every target price. It records the verified source commit and public URL with no changed files instead of retrying or blocking a no-op deployment.
- 2026-08-22: Bridge 1.8.20 validates recovered EC price plans by deterministic values rather than requiring regenerated `unit_evidence` prose to be byte-for-byte identical. Product identifiers, pricing rules, unit multipliers, price baselines, targets, and shipping modes remain locked. Amazon, Rakuten, Yahoo, Mercari Shops, BASE, Qoo10, and TikTok also share one bounded recovery policy: inspect the visible failure, apply at most two attempts of the smallest evidence-backed repair, and stop before any unverified product, price, inventory, shipping, tax, content, account, browser, or bulk change.
- 2026-08-22: Bridge 1.8.21 receives TSA's learned per-channel product titles, checks every mapped candidate before declaring a product unregistered, and continues other EC/LP updates when one channel is evidence-backed `not_found`.
- 2026-08-22: Bridge 1.8.22 receives server-locked EC identifiers for verified JANs and rejects `not_found` plans that ignore them. It also accepts a server allow-listed LP GitHub source when Vercel does not expose repository metadata, while still requiring a clean fresh clone whose HEAD matches the approved origin production branch.
- 2026-08-22: Bridge 1.8.19 recovers Amazon price writes blocked by error 90220. It may set only `この商品はAmazon.co.jp限定商品ですか？` to `いいえ` when the exact ASIN/SKU is explicitly recorded in the price-update Skill as the same product sold on other marketplaces. All other catalog attributes remain untouched; products without exact multi-market evidence still stop safely.
- 2026-08-22: Bridge 1.8.18 fixes the internal tab-ownership conflict between the separate read-only planning and write Codex sessions. Existing signed-in tabs remain the first choice, but a contended or missing seller tab now falls back automatically to one temporary tab in the same logged-in Chrome profile. The worker permits and counts only `chrome.tabs.new()` within the target-site limit, continues to reject other browser/window/profile creation, closes only its temporary tab, and no longer tells the operator to click a Chrome-top control that may not exist.
- 2026-08-22: Bridge 1.8.17 records the authenticated TSA administrator's exact product/site/price confirmation in each EC price job. The unattended write phase treats that record as the execution authorization and must save and verify without requesting an impossible chat reply. Batch reservations receive authorization when the administrator confirms `予約分をまとめて実行`. Historical duplicate-confirmation results are shown as stopped-without-save and can be retried after the Bridge update.
- 2026-08-21: Bridge 1.8.15 opens a visible, read-only PowerShell monitor for each claimed TSA job. It shows the task, product and EC targets, current browser step, progress, elapsed time, approximate completion window, last response age, and Bridge/Codex PIDs; closing the monitor does not stop the worker. EC price progress now carries the scoped browser operation title. Amazon price-only writes are pinned to the existing tab's `/interactive/listing/workflow/edit/offer` route so unrelated catalog validation such as error 90220 cannot be submitted with a price change.
- 2026-08-21: Bridge 1.8.14 makes a saved TSA recipe `product_lp_url` a mandatory part of EC price jobs. Planning now stops before external writes unless the LP product, old price, target occurrence, and Vercel-linked GitHub source are validated. Each job clones the current GitHub production branch into a fresh workspace, updates only the planned occurrences, deploys through the existing branch, and requires the registered public LP URL to show the new price before the job can complete.
- 2026-08-20: Bridge 1.8.13 retries every already-open official tab for the same EC when the newest match is owned by another browser session. A single locked duplicate no longer blocks a site while another signed-in existing tab is available. Read-only planning policy violations finish normally so claimed tabs are released instead of being stranded by a forced process kill; write-phase violations still stop before further external changes.
- 2026-08-20: Bridge 1.8.12 makes EC price jobs existing-tab-only. Both read-only planning and write prompts forbid new tabs/windows and temporary profiles, preserve operator tabs, and stop with the exact missing official tab instead of treating a new tab's expired session as a login failure. The two affected read-only jobs were cancelled as `価格変更なし`.
- 2026-08-20: Bridge 1.8.11 rejects older worker versions, preserves an active worker lease before claiming another job, and heartbeats immediately after claim. This prevents a second PC using the same worker ID from running Chrome jobs concurrently. EC price prompts and Skill now inspect one site at a time with scoped locators instead of full seller-dashboard snapshots. A duplicate-worker Native Host error was invalidated as `価格変更なし`.
- 2026-08-20: Bridge 1.8.10 added phase-specific EC price progress (`変更前価格を確認中` / `EC価格へ反映中`), concise failure summaries, and explicit no-write wording for planning failures. Amazon's informational new-feature modal is handled by the `update-aizu-ec-prices` Skill and is not treated as an authentication block. Installed locally and confirmed heartbeat with no active job.
- 2026-08-11: Bridge 1.8.0 added common Codex runtime discovery and version auditing for sales CSV, ad-cost, EC-settlement, and monthly-analysis jobs. Verified locally with Bridge 1.8.0 and `codex-cli 0.147.0-alpha.6.6`; the connection-test result stored the same runtime metadata, and the production TSA screen displayed both versions. Rakuten and Qoo10 July settlement jobs also completed without the previous missing-host error. Their official July reports remain unpublished, so TSA correctly keeps the estimates until the scheduled retry can obtain official data.
- 2026-08-11: The EC expense overview now separates live execution, queueing, operator waits, failures, and official-publication waits. Active rows show progress, current step, and the last Bridge response; a response older than two minutes is flagged as a possible stall. Verified that the latest Rakuten and Qoo10 retries finished and no jobs remained active.

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "summary", "new_catchcopy", "sites"],
  "properties": {
    "status": { "enum": ["completed", "waiting_for_user", "needs_review", "failed"] },
    "summary": { "type": "string" },
    "new_catchcopy": { "type": "string" },
    "sites": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["site", "status", "final_catchcopy", "product_identifier", "message"],
        "properties": {
          "site": { "enum": ["rakuten", "yahoo"] },
          "status": { "enum": ["updated", "submitted_pending", "not_found", "blocked"] },
          "final_catchcopy": { "type": ["string", "null"] },
          "product_identifier": { "type": ["string", "null"] },
          "message": { "type": "string" }
        }
      }
    }
  }
}

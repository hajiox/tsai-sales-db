{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "summary", "sites"],
  "properties": {
    "status": { "enum": ["ready", "waiting_for_user", "needs_review", "failed"] },
    "summary": { "type": "string" },
    "sites": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["site", "status", "observed_catchcopy", "target_catchcopy", "product_identifier", "message"],
        "properties": {
          "site": { "enum": ["rakuten", "yahoo"] },
          "status": { "enum": ["planned", "not_found", "blocked"] },
          "observed_catchcopy": { "type": ["string", "null"] },
          "target_catchcopy": { "type": ["string", "null"] },
          "product_identifier": { "type": ["string", "null"] },
          "message": { "type": "string" }
        }
      }
    }
  }
}

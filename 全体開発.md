# TSAシステム 全体開発経過

## 最終更新: 2026-03-05

---

## 1. プロジェクト概要

- **プロジェクト名:** tsai-sales-db (TSAシステム)
- **リポジトリ:** https://github.com/hajiox/tsai-sales-db
- **本番環境:** https://v0-tsa-19.vercel.app （Vercel。GitHub mainブランチへのpushで自動デプロイ）
- **テスト環境:** localhost:3001 （PM2管理。ローカル検証専用、本番ではない）
- **DB:** Supabase（本番・テスト共通）
- **技術スタック:** Next.js, Supabase, Tailwind CSS, shadcn/ui, Lucide React
- **ワークスペース:** `C:\作業用\tsai-sales-db`

> **⚠️ デプロイ注意:** 本番はVercel。コード変更を反映するにはGitHubへpushすること。ローカルのPM2再起動は本番には反映されない。

---

## 2. レシピ印刷レイアウト修正 (2026-02-15 〜 02-16)

### 2-1. 目的
レシピ詳細ページ (`/recipe/[id]`) のA4印刷レイアウトを改善する。

### 2-2. 実施した変更

####  A4横向き (Landscape) 対応
- **ファイル:** `app/recipe/[id]/page.tsx`
- **変更:** `@page { size: A4 portrait; }`  `@page { size: A4 landscape; }`
- **理由:** 5列（#, 名称, 基本(1), A, B）を収めるために横幅が必要

####  バッチ量列 A/B の表示修正
- **問題:** `400 (A)` と `800 (B)` の列がHTMLに存在するが印刷時に消える
- **原因:** `globals.css` の `@media print` で `.bg-gray-50` が `display: none !important` に設定されていた
- **対処:** 印刷用テーブルのth/tdから `bg-gray-50` クラスを全て削除
- **ヘッダー形式:** `{batchSize1} (A)`  `A ({batchSize1})` に変更

####  32行対応の余白圧縮
- **目標:** A4横1ページに最大32行の材料を収める
- **圧縮前:** ヘッダー161px + 行20px  最大22行
- **圧縮後:** ヘッダー56px + 行17px  最大39行（32行は余裕で収まる）
- **具体的な変更:**
  - タイトル: `text-2xl`  `text-sm`, `leading-none`
  - スペック情報（充填量表記量保存方法殺菌）を1行にまとめた
  - テーブルセルのpadding: `py-1`  `py-0`
  - フォントサイズ: `text-xs`  `text-[10px]`
  - グループ見出し製造メモの余白も全て圧縮
  - グローバルCSS印刷td paddingを上書き: `padding: 1px 4px !important`

####  材料経費グループの非表示
- `groupedItems.map`  `groupedItems.filter(g => g.type !== 'material' && g.type !== 'expense').map`
- 印刷時は原材料と経費グループを除外

### 2-3. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/recipe/[id]/page.tsx` | 印刷レイアウト全体（ヘッダー圧縮、bg-gray-50削除、A/Bヘッダー変更） |
| `app/globals.css` | `.bg-gray-50` が印刷時 `display:none` になっている（未修正。代わりにpage.tsx側で対処） |

### 2-4. 最終コミット
- **コミット:** `184ae18` - "fix: A/B headers and remove bg-gray-50 from print"
- **ブランチ:** main
- **デプロイ状態:** Vercel自動デプロイ済み

### 2-5. 既知の問題
- **Antigravityファイル巻き戻しバグ:** Antigravityが `page.tsx` を頻繁に古い状態に戻す。対策として `[System.IO.File]::ReadAllText/WriteAllText` で直接ファイル操作後、即座に `git commit & push` するワークフローを採用。
- **PowerShellの文字問題:** `` (U+00D7) をReplaceする際、PowerShellリテラルでは `[char]0x00D7` を使う必要がある。
- **git showパイプのエンコーディング破損:** `git show HEAD:"file" | Out-String` でUTF-8マルチバイト文字が化ける。git showからパイプでファイル生成するのは危険。

---

## 3. レシピ計算・管理機能の強化 (2026-02-17)

### 3-1. 目的
レシピの重量計算の正確性を向上させ、Amazon手数料や人件費などの変動要素をユーザーが管理できるようにする。

### 3-2. 実施した変更

#### 全体重量（原材料 + 中間加工品）の表示位置修正
- **内容:** 画面およびPDF印刷レイアウトにおいて、「原材料」と「中間加工品」セクションの直後に「全体重量」のサマリーを表示するように移動。
- **理由:** 材料の構成を確認した後、すぐに合計重量を把握できるようにするため。

#### 中間加工品の単位変更（g → 個）
- **内容:** 中間加工品をレシピに組み込む際の単位を「g」から「個」に変更。
- **計算ロジック:** `個数 × 中間加工品レシピの合計重量` で全体重量に加算されるよう修正。

#### 材料データベースの機能拡張
- **税込・税抜設定のカラム追加:** `ingredients`（食材）および `materials`（資材）テーブルに `tax_included` カラムを追加し、保存時のエラーを解消。
- **諸経費（Expenses）管理:** 新たに `expenses` テーブルをDBに作成し、DB管理画面に「諸経費」タブを追加。送料や人件費をマスターデータとして管理可能にした。
- **Amazon手数料率の動的設定:** データベース画面上部で Amazon手数料（%）を自由に設定・保存可能にし、全レシピの原価計算にリアルタイム反映。

### 3-3. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/recipe/[id]/page.tsx` | 全体重量の表示位置修正、Amazon手数料の動的計算ロジック、諸経費取得 |
| `app/recipe/new/page.tsx` | 新規作成時の重量計算、中間加工品の個数対応、販売価格変更時のAmazon手数料自動計算 |
| `app/recipe/database/page.tsx` | 諸経費のCRUD管理、税込切替機能、Amazon手数料率の設定UI実装 |

---

## 5. ワークスペース移行 (2026-02-16)

### 5-1. 変更内容
- **旧パス:** `C:\Users\ts\OneDrive\Desktop\作業用\tsai-sales-db`
- **新パス:** `C:\作業用\tsai-sales-db`
- **理由:** デスクトップ（OneDrive配下）での挙動不安定を回避

### 5-2. 移行手順
1. `C:\作業用` フォルダを作成済み
2. `Copy-Item` でプロジェクト全体をコピー済み
3. Gitリポジトリコミット履歴もそのまま保持
4. Antigravityで「ファイル -> フォルダーを開く -> C:\作業用」で切り替え

---

## 6. 過去の開発履歴（概要）

### 日付フィルタリング (2026-02-03〜05)
- ダッシュボード分析に日付フィルタリングを実装

### 画像アップロード (2026-02-01〜03)
- リンク管理システムに画像アップロード機能を追加
- Supabase Storageへの画像アップロードAPI

### チャットボットUI修正 (2026-02-01〜02)
- チャットボットインターフェースのUI重なり修正

### Shopeeチャットボット (2026-01-26〜02-01)
- ShopeeチャットボットのAPI依存関係の修正

---

## 7. 技術メモ

### Antigravityでのファイル編集ワークフロー（推奨）
```powershell
# 1. ファイル読み込み
$file = 'path/to/file.tsx'
$c = [System.IO.File]::ReadAllText($file)

# 2. Unicode文字を含む置換（記号など）
$mult = [char]0x00D7
$c = $c.Replace("old${mult}text", 'new_text')

# 3. 書き込み＆即座にコミット
[System.IO.File]::WriteAllText($file, $c)
git add $file
git commit -m "message"
git push origin main
```

### globals.css 印刷設定の注意点
- `globals.css` の `@media print` で以下が `display: none !important`:
  - `nav`, `button`, `.no-print`, `.bg-gray-50`, `.shadow`, `.rounded-lg`
  - `input[type="month"]`, `input[type="checkbox"]`
- **印刷用レイアウトでこれらのクラスを使うと消える** ので注意
- レシピページは `<style jsx global>` で独自の印刷スタイルを上書きしている

### Supabase接続
- プロジェクトURLキーは環境変数で管理
- テーブル: recipes, recipe_items, etc.

## 諸経費データの整理移行 (2026-02-17)

### 目的
資材データベース(materials)に混在していた「送料」「人件費」「手数料」などの諸経費データを、新設された諸経費データベース(expenses)へ移行し、マスター管理の適正化を図る。

### 実施した変更
- **データ移行**: 23件の諸経費候補（送料、ヤマト、佐川、人件費、OEM手数料など）を資材から諸経費テーブルへ移行。
- **リレーション更新**: レシピ項目(ecipe_items)の item_type を material から expense へ一括更新。
- **データクリーンアップ**: 移行済みの資材レコードを削除。

### 影響範囲
- レシピ詳細画面: 移行したアイテムは「諸経費」セクションに表示されるようになる。
- 原価計算: expense として計算されるため、重量（g）計算からは除外され、正確な全体重量が算出されるようになる。

## 見積書AI解析自動価格更新機能の実装 (2026-02-17)

### 目的
見積書（PDFやFAXのスキャン画像）をAIで解析し、商品名や単価を抽出、データベース内の既存項目と自動的に照合して価格を更新する作業を効率化する。

### 実装内容
- **バックエンド (Gemini 2.0 Flash)**:
  - 画像PDFから商品名と価格を抽出。
  - 既存の食材資材データベースとの「ゆらぎ」を考慮した名称照合（名寄せ）。
  - FAXなどの低品質画像に対する高度なOCRと名称推測。
- **フロントエンド UI**:
  - 見積書アップロード機能。
  - AIによる更新案のレビューテーブル（現在の価格 vs 新価格を比較表示）。
  - ユーザーの確認後に初めてDBを更新する安全設計。
  - 該当する商品がない場合に「新規登録」を提案。
- **コンポーネント**:
  - Badge コンポーネントの新規追加。

### 関連ファイル
-  pp/recipe/database/quote-import/page.tsx: 見積書解析更新UI
-  pp/api/quote/analyze/route.ts: Geminiによる解析API
-  pp/api/quote/update/route.ts: DB更新実行API
- components/ui/badge.tsx: 状況表示用バッジ

## 8. Amazon手数料の諸経費からの分離 (2026-02-18)

### 8-1. 目的
Amazon手数料（販売価格の%）が諸経費に混在し、正確に計算されないレシピがあった。Amazon手数料を諸経費セクションから分離し、レシピごとにON/OFFで管理できるようにする。

### 8-2. 実施した変更

#### DBスキーマ変更
- **カラム追加**: `recipes`テーブルに`amazon_fee_enabled` (BOOLEAN, DEFAULT false) を追加
- **SQL**: `ALTER TABLE recipes ADD COLUMN IF NOT EXISTS amazon_fee_enabled BOOLEAN DEFAULT false;`

#### Amazon手数料トグルスイッチの追加
- Pricing Card（ダークテーマの価格表示カード）内にトグルスイッチを追加
- ON/OFF切替でAmazon手数料の有無をレシピごとに設定可能
- ON時: 販売価格（税込） x Amazon手数料率(%) = Amazon手数料額を自動計算
- 手数料率はデータベース管理画面のグローバル設定（localStorage）から取得

#### 原価計算ロジックの分離
- `getTotals()`関数を改修し、items内の旧「Amazon手数料」expenseを除外して計算
- `amazonFee`を独立した計算値として管理
- 原価合計 = 材料費 + Amazon手数料（フラグON時のみ）
- 粗利益の計算もAmazon手数料込みで正確に反映

#### 原価内訳UIの追加
- Amazon手数料ON時、原価内訳を表示:
  - 材料資材経費: XXX
  - Amazon手数料 (10%): XXX

### 8-3. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/recipe/[id]/page.tsx` | トグルUI、原価計算ロジック分離、内訳表示、保存処理更新 |
| Supabase SQL Editor | `amazon_fee_enabled`カラム追加 |

### 8-4. コミット
- **コミット:** `878e517` - "feat: separate Amazon fee from expenses with toggle switch"
- **ブランチ:** main
- **デプロイ状態:** Vercel自動デプロイ


## 9. psql (PostgreSQLクライアント) のインストール (2026-02-18)

### 9-1. 目的
Supabase REST API (PostgREST) はDDL（ALTER TABLE等のスキーマ変更）を実行できないため、DBマイグレーション時にブラウザでSupabaseダッシュボードのSQL Editorを開く必要があった。これは非効率でAIエージェントとの連携も困難だった。`psql`をローカルにインストールし、コマンドラインから直接SQLを実行可能にする。

### 9-2. インストール内容

| 項目 | 値 |
|------|------|
| ツール | psql (PostgreSQL 17.8) |
| インストール方法 | `winget install PostgreSQL.PostgreSQL.17` |
| インストール先 | `C:\Program Files\PostgreSQL\17\bin\` |
| PATH追加 | ユーザーPATHに追加済み |

### 9-3. 接続コマンド

```powershell
$env:PAGER=""; psql -h aws-0-ap-southeast-1.pooler.supabase.com -p 6543 -U postgres.zrerpexdsaxqztqqrwwv -d postgres --no-psqlrc -P pager=off -c "<SQL>"
```

### 9-4. 注意事項
- `PAGER`環境変数を空にする（Windowsに`cat`コマンドが無いためエラーになる）
- `--no-psqlrc`と`-P pager=off`を必ず付与
- UTF-8エンコーディング確保のため`[Console]::OutputEncoding`を設定
- ワークフロー: `.agent/workflows/db-migration.md`に手順を記載済み

### 9-5. 背景
Supabase REST APIが使えなかった理由:
1. PostgRESTはCRUD操作のみ対応。DDL（ALTER TABLE等）は実行不可
2. `rpc/exec_sql`のようなカスタム関数はデフォルトで存在しない
3. Supabase Management APIは別のアクセストークンが必要

---

## 10. AI原材料ラベル生成機能の実装 (2026-02-19)

### 10-1. 目的
食品表示法（2025年最新基準）に準拠した原材料名を自動生成し、商品ラベル作成業務を効率化する。また、複合原材料の成分データを一元管理し、表記の揺れや抜け漏れを防ぐ。

### 10-2. 実装内容

#### インライン生成・編集UI
- **ファイル:** `app/recipe/[id]/page.tsx`
- **変更:** 商品名の右側にインラインの原材料表示パネルを追加。
- **機能:**
  - **AI生成:** Gemini APIを使用し、レシピ構成から原材料名を自動生成。
  - **インライン編集:** 生成結果を確認・修正してから保存可能。自動保存は廃止し、安全性を重視。
  - **警告表示:** 不足データの警告（例: アレルギー情報の欠落）を表示。
  - **削除機能:** ゴミ箱アイコンで原材料表示をリセット。

#### Gemini AI プロンプト設計
- **ファイル:** `app/api/recipe/generate-label/route.ts`
- **ロジック:**
  - レシピ内の食材(ingredient)と中間加工品(intermediate)を展開し、DBの `raw_materials`（原材料構成）を取得。
  - 重量順、複合原材料の展開、添加物の分離（/以下）、アレルギー表示（末尾）などのルールを適用。
  - **安全性強化:**
    - 原材料データがない複合原材料（例: 〇〇麺、〇〇ソース）について、AIによる推測補完を**厳格に禁止**。
    - 代わりに `【要確認: 〇〇】` というプレースホルダーを出力し、ユーザーに手動確認を促す仕様に変更。これは食品事故防止のため必須の措置。

#### データベース連携
- **Fuzzy Matching (あいまい検索):**
  - レシピ上の食材名（例: "バーモンドカレー"）とDB上の正式名（例: "バーモントフレーク"）の表記ゆれを吸収。
  - 正規化（スペース削除、半角全角統一）に加え、ブランド名抽出やLCS（最長共通部分列）によるマッチングを実装。

### 10-3. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/api/recipe/generate-label/route.ts` | AI生成ロジック、ファジーマッチング、プロンプト強化 |
| `app/recipe/[id]/page.tsx` | インラインUI、編集・保存・削除機能 |

---

## 11. ラベルインポート機能の改善 (2026-02-19)

### 11-1. 目的
商品ラベル（写真）から原材料データをOCRで取り込み、既存のデータベースを更新する際、データの整合性を保ちながら効率的に入力作業を行う。

### 11-2. 実施した変更

#### 名前上書き防止ロジック
- **問題:** 更新モードでラベルOCR結果（例: "バーモントフレーク"）が、ユーザーにとっての管理名（例: "バーモンドカレー"）を上書きしてしまう事故が発生。
- **解決策:** `actionMode === "update"` の場合、`name` フィールドをデフォルトで**選択解除**状態にする修正を実施。ユーザーが明示的にチェックしない限り、管理名は変更されない。

#### データ不整合の解消
- `ingredients` テーブルの一部データ（バーモンドカレー、ジャワカレー等）の名前を修正し、レシピデータとの整合性を回復。

### 11-3. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/recipe/database/label-import/page.tsx` | 更新時のフィールド選択ロジック修正 |

---

## 12. Excel vs DB 全数データ検証・修正 (2026-02-20)

### 12-1. 目的
ExcelファイルとSupabase DBのレシピデータ（usage_amount, unit_quantity, unit_price, cost等）を全数照合し、不整合を特定・修正する。

### 12-2. 実施した変更

#### 全数照合スクリプトの作成
- 全5Excelファイル（282シート）と DB全366レシピ（4,709アイテム）を完全照合
- Supabase API 1000件制限をページネーションで回避
- Excelヘッダー行位置の動的検出、シート名⇔DB名のファジーマッチング

#### 修正済みバグ（計5件）
1. **南会津産トマトと赤ワインのキーマカレー（通常版）**: 全16材料のusage_amountが1kg業務用版の値（5.7倍）になっていた
2. **南会津産トマトと赤ワインのキーマカレー（1kg版）**: 全16材料のusage_amountが初期値1のままだった
3. **桃バター 香料（桃）**: usage_amountが10倍ズレ（1.1→0.11）
4. **つけ麺スープ / つけ麺スープ（極にぼ）**: 3材料のデータが2レシピ間で入れ替わっていた
5. **喜多方背脂2食**: 6食版の値が入っていた（前回対応済み）

#### 配合差異の修正（15件）
- なみえ豚カレー、辛すぎInspire零、ももの葉茶4個セット、えちご家3個、ウニふりかけ、豚角煮、蕎麦ふりかけ等をExcel値に統一

### 12-3. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `verify_all.mjs` | Excel全数照合スクリプト（作業後削除済み） |
| `fix_all.mjs` | 修正スクリプト（作業後削除済み） |

---

## 13. サイドバーUI修正 (2026-02-20)

### 13-1. 目的
メインサイドバーで選択中のシステム名が白背景＋白文字で読めない問題を修正する。

### 13-2. 実施した変更

#### アクティブ状態のスタイル修正
- **原因:** shadcn/ui `variant="secondary"`（白背景）と `text-white` クラスが競合
- **修正:** `variant="secondary"` を廃止し、全ボタンを `variant="ghost"` に統一。アクティブ判定は `className` で制御:
  - アクティブ: `bg-slate-600 text-white font-semibold hover:bg-slate-500`
  - 非アクティブ: `text-slate-200 hover:bg-slate-700 hover:text-white`

### 13-3. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `components/main-sidebar.tsx` | baseBtn関数化、アクティブスタイル修正 |

---

## 14. WEB販売ダッシュボード ホバーツールチップ修正 (2026-02-20)

### 14-1. 目的
WEB販売ダッシュボードの各要素（サマリーカード、データテーブル）にマウスホバーした際に表示されるトレンドツールチップが正しく動作しない問題を修正する。

### 14-2. 実施した変更

#### DB関数の再構築（5関数）
- `get_total_trend_data`, `get_site_trend_data`, `get_series_trend_data`: `months` CTE + 純粋な `LEFT JOIN` に再構築し、データが無い月でも `0` を返すように修正
- `get_product_trend_data`: `date` 型引数 → `text` 型に変更（他の関数と統一）。内部で `(target_month || '-01')::date` にキャスト
- `get_product_site_trend_data`: `months` CTE + `LEFT JOIN` で6ヶ月分を確実に返すように再構築
- 全関数で `COALESCE(ws.tiktok_count, 0)` を追加（後から追加されたカラムの `NULL` 対策）

#### tiktok_countデータのバックフィル
- 2025年9月以前の3143行で `tiktok_count = NULL` だったデータを `0` に `UPDATE`

#### フロントエンドのRPC呼び出し修正
- `getCurrentMonth()`: `"${month}-01"` → `month`（`"YYYY-MM"` 形式）に統一。DB関数が内部で `-01` を追加するため二重追加を防止
- ECサイト別セルホバー: `site.key`（`"amazon_count"`）から `_count` を除去してRPCに渡すように修正
- stateキーの統一: `hoveredSiteCell` と `siteTrendData` のキー形式を `"uuid-amazon_count"` で統一

### 14-3. 修正したバグの詳細
| # | 箇所 | 原因 | 修正 |
|---|------|------|------|
| 1 | サマリーカード | LEFT JOIN + WHERE が INNER JOIN 相当に | months CTE + 純粋 LEFT JOIN |
| 2 | サマリーカード | tiktok_count NULL → SUM全体NULL | COALESCE + データバックフィル |
| 3 | ECサイト別セル | 日付 `"YYYY-MM-01-01"` 二重追加 | `"YYYY-MM"` 形式で統一 |
| 4 | ECサイト別セル | カラム名 `"amazon_count_count"` 二重追加 | `_count` 除去してRPC呼び出し |
| 5 | ECサイト別セル | stateキー不一致でデータ参照失敗 | キー形式統一 |
| 6 | 商品名 | DB関数 `date` 型 vs フロント `text` 型不一致 | DB関数を `text` 型に変更 |

### 14-4. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `components/WebSalesDataTable.tsx` | ホバー処理・RPC呼び出し・stateキー統一 |
| `components/websales-summary-cards.tsx` | サマリーカードのトレンド取得 |
| DB関数 (5件) | `get_total/site/series/product/product_site_trend_data` |

---

## 15. ヤマト出荷データ管理システム 広告用顧客リストCSVエクスポート機能 (2026-02-22)

### 15-1. 目的
ヤマト出荷データから抽出したリピーター顧客リストを、Google Ads（カスタマーマッチ）およびMeta（Instagram/Facebook カスタムオーディエンス）にそのままアップロードできるCSV形式でエクスポートする機能を実装する。

### 15-2. 実施した変更

#### バックエンドAPI (`app/api/export/route.ts`)
- **電話番号変換**: 国内形式 (0XXXXXXXXXX) → E.164形式
  - Google用: `+81XXXXXXXXXX`
  - Meta用: `81XXXXXXXXXX`（+なし）
- **日本語姓名分割**: スペース区切り（全角・半角）対応 + スペースなし漢字名の推定分割（2文字姓デフォルト、3文字姓リスト対応）
- **除外顧客フィルタ**: `ignored_customers` テーブルに登録済みの顧客を自動除外
- **抽出条件**: 購入回数（注文日数ベース）3回以上 / 5回以上 / 10回以上
- **BOM付きUTF-8**: Excel互換のCSV出力

#### Google用CSV出力仕様
| ヘッダー | 内容 |
|----------|------|
| Email | 空欄（DBにEmailデータなし） |
| Phone | +81始まりE.164形式、ハイフンなし |
| First Name | 名 |
| Last Name | 姓 |
| Country | "JP"固定 |
| Zip | ハイフンなし7桁 |

#### Meta用CSV出力仕様
| ヘッダー | 内容 |
|----------|------|
| email | 空欄 |
| phone | 81始まり（+なし）、ハイフンなし |
| fn | 名 |
| ln | 姓 |
| ct | "JP"固定 |
| zip | ハイフンなし7桁 |
| value | 累計購入回数（LTV代替値） |
| currency | "JPY"固定 |

#### フロントエンドUI (`app/page.tsx`)
- ロイヤル顧客分析セクションの直下に「広告用 顧客リストエクスポート」セクションを追加
- Google Ads / Meta の2カラムレイアウト
- 各プラットフォームに「3回以上」「5回以上」「10回以上」のダウンロードボタン
- ボタンに対象顧客数を動的表示（例: 10,499人）
- ダウンロード結果のフィードバック表示

### 15-3. 対象顧客数（実装時点）
| 条件 | 顧客数 |
|------|--------|
| 3回以上 | 10,499人 |
| 5回以上 | 3,979人 |
| 10回以上 | 874人 |

### 15-4. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `yamato-analytics/app/api/export/route.ts` | CSV生成API（新規作成） |
| `yamato-analytics/app/page.tsx` | エクスポートUI追加 |

### 15-5. 注意事項
- **Email列**: DBに保存されていないため空欄。Emailデータ取得元があれば別途対応可能
- **MetaのLTV (value)**: 金額データがないため購入回数を代替出力。売上金額データを持たせれば修正可能
- **無効な電話番号**: 全0、1桁、10桁未満の番号は自動スキップ

---

## 16. ギフト注文者CSVエクスポート機能 (2026-02-23)

### 16-1. 目的
備考欄に「〇〇様ご依頼分」と記載されたギフト注文の**依頼者（ギフト主）**を抽出し、広告用顧客リストとしてCSV出力する。

### 16-2. データ分析結果
- 備考に「ご依頼分」を含む伝票: **10,653件**
- ユニーク依頼者数: **6,958人**（名前ベースで正規化）
- 2回以上ギフト利用者: **1,440人**
- 年別推移: 2020年117件 → 2025年3,494件（年々増加傾向）
- ギフトで最も人気の商品: チャーシュー系（800g / 訳アリ1Kg / カット済）

### 16-3. バックエンド実装
- **ファイル:** `yamato-analytics/app/api/export/route.ts`
- `type=gift` クエリパラメータを追加
- 備考から「〇〇様ご依頼分」パターンで依頼者名を正規表現で抽出（`extractRequesterName()`）
- 依頼者名を正規化（スペース除去）してグループ化し、ギフト回数をカウント
- `minOrders=1`（全体）/ `minOrders=2`（2回以上）でフィルタ
- **注意:** ギフト主の電話番号はDBに存在しない（受取人の番号のみ）→ Phone列は空欄

### 16-4. ギフト統計API
- **ファイル:** `yamato-analytics/app/api/gift-stats/route.ts`（新規）
- ギフト依頼者の総数（6,958人）と2回以上利用者数（1,440人）を返すエンドポイント
- フロントエンドのボタンに人数を動的表示するために使用

### 16-5. フロントエンドUI
- **ファイル:** `yamato-analytics/app/page.tsx`
- エクスポートセクションを2カテゴリに整理:
  - **👥 リピーター顧客**（既存）: 青/紫ボタン、3回/5回/10回以上
  - **🎁 ギフト注文者**（新規）: ピンクボタン、全体/2回以上
- 各カテゴリにGoogle Ads / Meta の2カラムレイアウト
- 補足注記: 「ギフト主の電話番号はDBに含まれないため、Phone列は空欄。名前・郵便番号でのマッチング用」

### 16-6. 対象顧客数（実装時点）
| 条件 | ギフト依頼者数 |
|------|--------|
| 全体 | 6,958人 |
| 2回以上 | 1,440人 |

### 16-7. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `yamato-analytics/app/api/export/route.ts` | `type=gift`対応・依頼者名抽出ロジック追加 |
| `yamato-analytics/app/api/gift-stats/route.ts` | ギフト統計API（新規作成） |
| `yamato-analytics/app/page.tsx` | ギフトエクスポートUI追加（ピンクボタン） |

---

## 17. ECサイトインポートAPI修正・商品データ整備 (2026-02-25)

### 17-1. 目的
各ECサイト（Amazon/楽天/Yahoo/メルカリ/BASE/Qoo10）の売上CSVインポート機能のバグ修正と、マッチング精度向上。あわせて商品マスタの重複データを整理する。

### 17-2. インポートAPI修正

#### 終売商品のマッチング除外
- **問題:** `is_hidden=true`（終売）の商品もマッチング候補に含まれており、廃番商品に誤マッチする可能性があった
- **修正:** 全6APIの `products` 取得クエリに `.eq('is_hidden', false)` を追加
- **対象ファイル:** `amazon-parse`, `rakuten-parse`, `yahoo-parse`, `mercari-parse`, `base-parse`, `qoo10-parse`

#### BASEクーポン行の除外
- **問題:** BASE CSVの「クーポン〇〇円OFF」等の行もCSV内に含まれ、未マッチとして処理されていた
- **修正:** `base-parse` にてタイトルが「クーポン」を含む・「割引」で始まる行をスキップ

#### メルカリCSV集計APIのリクエスト形式修正
- **問題:** `aggregate/mercari-csv` が JSON (`{ csvContent }`) を期待するがクライアントが `multipart/form-data` で送信 → 500エラー
- **修正:** クライアント側テストスクリプトを JSON 送信に変更（API仕様に合わせる）

#### HTMLタグ正規化（csvHelpers.ts）
- **問題:** 楽天の商品名に `<br>` や HTMLエンティティが混入しており、正規化前にマッチングが失敗するケースがあった
- **修正:** `normalizeTitle()` 関数の冒頭で `<br>` 等のHTMLタグをスペースに置換、その後 `&nbsp;` 等のエンティティを除去

### 17-3. マッチング精度テスト結果（修正後）
| EC | 合計種 | マッチ | 未マッチ | 備考 |
|----|--------|--------|---------|------|
| Amazon | 80 | 78 | 2 | 極長商品名 |
| 楽天 | 90 | 87 | 3 | 極長商品名 |
| Yahoo | 80 | 76 | 4 | 業務用・ギフト系 |
| メルカリ | — | ✅ | — | 500エラー解消 |
| BASE | — | ✅ | — | 500エラー解消・クーポン除外 |
| Qoo10 | — | ✅ | — | 500エラー解消 |

### 17-4. 商品マスタ整備（DB直接操作）
- **背景:** 商品名変更（訳アリ1Kg/5個セット → 800g/4個セット）をスタッフが誤って「新規商品登録」で対応したため、重複レコードが発生
- **旧商品 ID `b9671a5c`:** 売上31件・学習マッピング6件・レシピ0件（正しいレコード）
- **誤登録 ID `19b8abc6`:** 売上2件・レシピ1件・yahoo_mapping1件・price_history1件（削除対象）
- **実施した作業:**
  1. 旧商品の `name` を新しい商品名にリネーム（`products.name` UPDATE）
  2. 誤登録側の `recipes.linked_product_id` を旧商品IDに付け替え
  3. 誤登録側の `web_sales_summary.product_id` を付け替え（1ヶ月分は既存データとマージ加算）
  4. `product_price_history`・`yahoo_product_mapping` も付け替え
  5. 誤登録商品レコードを `DELETE`
- **結果:** 旧IDが維持されたため過去31ヶ月分の売上・ECマッピング学習データ・レシピが全て継続参照可能

### 17-5. アーキテクチャ補足（リネームが安全な理由）
- `products.id`（UUID）を外部キーとして全テーブルが参照しているため、`name`フィールドのみ変更しても全連携は維持される
- `web_sales_summary`, `recipes`, `*_product_mapping` はすべて `product_id` で紐付け
- `register/route.ts` のみ `products.name` をキーとして使うが、リネーム後は新名前でマッチング結果が引き当てられるため問題なし

### 17-6. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `lib/csvHelpers.ts` | `normalizeTitle()` にHTMLタグ・エンティティ除去を追加 |
| `app/api/import/base-parse/route.ts` | クーポン行スキップ・`is_hidden`フィルタ追加 |
| `app/api/import/qoo10-parse/route.ts` | `is_hidden`フィルタ追加 |
| 全6 parse API | `is_hidden=false`フィルタ追加 |
| `scripts/fix-product-merge.mjs` | 商品マージ作業スクリプト（参考保存） |

---

## 18. レシピ管理機能の全面修正 — RLSバイパス・原価計算・テーブル間移動 (2026-02-26)

### 18-1. 目的
フロントエンドからの直接Supabase書き込みがRow Level Security (RLS)で無言ブロックされ、レシピの全書き込み操作（コピー・削除・保存・データベース更新）が失敗していた問題を解決する。併せて、資材・諸経費の原価計算バグとUI欠落を修正する。

### 18-2. RLSバイパスのためのAPI移行

#### 新規API
| API | メソッド | 用途 |
|-----|---------|------|
| `/api/recipe/update` | PATCH | レシピフィールド更新（名前・カテゴリ・シリーズ・商品番号等） |
| `/api/recipe/update` | POST | レシピ複製（全アイテムのディープコピー） |
| `/api/recipe/save` | POST | レシピ詳細の一括保存（アイテム追加/更新/削除 + メタデータ + WEB販売商品への自動同期） |
| `/api/recipe/db-write` | POST | 汎用テーブル書き込み（ingredients/materials/expenses の insert/update/delete） |

#### 移行対象ページ・操作
| ページ | 移行した操作 |
|--------|------------|
| `recipe/page.tsx` | カテゴリ変更、開発日変更、シリーズ変更、商品番号変更、名前変更、複製、削除 |
| `recipe/[id]/page.tsx` | カテゴリ変更、フィールド更新、全体保存、原材料表示削除 |
| `recipe/database/page.tsx` | 食材/資材の追加・編集・削除、税込切替、一括税設定 |
| `recipe/ingredients/page.tsx` | 追加・編集・削除 |

#### 設計方針
- `SUPABASE_SERVICE_ROLE_KEY` はバックエンドAPI内でのみ使用（フロントエンドに露出しない）
- RLSポリシーは読み取りには維持、書き込みのみAPI経由で管理操作

### 18-3. 資材・諸経費の原価計算修正

#### 修正したバグ
| # | 問題 | 原因 | 修正内容 |
|---|------|------|----------|
| 1 | 資材・諸経費の使用量入力欄がない | `isMaterialGroup` 条件で使用量欄を非表示（`-`）にしていた | 全タイプで使用量入力欄を表示、プルダウン選択時にデフォルト`usage_amount=1` |
| 2 | 資材の`unit_quantity`が文字列→コスト計算スキップ | materialsテーブルの`unit_quantity`がtext型（"3000"等） | マスター読み込み時に`parseFloat`で数値変換、デフォルト1 |
| 3 | 資材の原価が¥0になる（**根本原因**） | 食材と同じ`usage × (price / unit_quantity)`で計算 → ダンボール¥29.7÷3000=¥0 | **資材・経費**: `usage × price`（1個単価）、**食材**: `usage × (price / unit_quantity)`（g単価）に分岐 |
| 4 | 資材の400個分・800個分が`-`表示 | `isMaterialGroup`で非表示 | 「個」単位で表示に修正 |

#### コスト計算式の整理
```
食材:   cost = usage_amount(g) × (price(パック価格) / unit_quantity(パック入数g)) × tax_rate
資材:   cost = usage_amount(個) × price(1個単価) × tax_rate
諸経費: cost = usage_amount(回) × unit_price(1回単価) × tax_rate
```

### 18-4. 材料データベースのテーブル間移動機能

#### 実装内容
- **資材タブ**: 各行の右端に「→諸経費」ボタン（オレンジ色）を追加
- **諸経費タブ**: 各行の右端に「→資材」ボタン（青色）を追加
- クリック時: 確認ダイアログ → 移動先テーブルにinsert → 元テーブルからdelete → 画面リフレッシュ
- **背景**: 資材(materials)テーブルに水道光熱費等の諸経費が混在していたため

### 18-5. UI改善
- 食材・資材選択プルダウンの幅を320px → 500pxに拡大し、長い名前が切れないよう修正
- レシピ詳細の編集モードにアイテムタイプ変更セレクト（食材/資材/中間部品/商品/諸経費）を追加
- レシピ重複削除API改善（紐付済みレシピも削除可能に、削除前に自動で紐付け解除）

### 18-6. 関連ファイル
| ファイル | 変更内容 |
|---------|---------| 
| `app/api/recipe/update/route.ts` | レシピ更新・複製API（新規） |
| `app/api/recipe/save/route.ts` | レシピ詳細保存API（新規） |
| `app/api/recipe/db-write/route.ts` | 汎用DB書き込みAPI（新規） |
| `app/api/recipe/duplicates/route.ts` | 重複削除API改善 |
| `app/recipe/page.tsx` | 一覧のコピー・削除をAPI経由に |
| `app/recipe/[id]/page.tsx` | 保存・更新をAPI経由に、原価計算修正、タイプ変更セレクト |
| `app/recipe/database/page.tsx` | 全書き込みをAPI経由に、テーブル間移動ボタン |
| `app/recipe/ingredients/page.tsx` | 全書き込みをAPI経由に |
| `app/recipe/_components/ItemNameSelect.tsx` | プルダウン幅拡大（500px） |


---

## 19. レシピ原価照合・中間部品データ整備・UI改善 (2026-02-27)

### 19-1. 目的
ExcelシートとDBレシピデータの原価整合性を完全に検証し、中間部品（【P】プレフィックス付きレシピ）のデータ品質を改善する。また、レシピ詳細ページに原価小計表示を追加する。

### 19-2. Excel-DB原価照合

#### 実施内容
- Excel全シートとDB全レシピの原価率を照合、レシピ名70件をExcelシート名に合わせてリネーム
- 重複レシピ6件を削除、新規レシピ35件をExcelから自動登録
- 資材・諸経費セクションのパース改善（セル結合・カラム位置ずれ対応）

#### 結果
- 初回: 91/329レシピで>20%差異
- 修正後: 46レシピに改善（うち34件はDB>Excelで正常＝中間部品展開差、残12件は要調査）

### 19-3. 中間部品の全面データ修正

#### 修正内容
| # | 問題 | 対処 | 件数 |
|---|------|------|------|
| 1 | 手順テキストが食材として誤登録 | 削除 | 6件 |
| 2 | 包装資材がingredientに誤分類 | materialに変更 | 7件 |
| 3 | 中間部品アイテムにゴミ・不正データ混在 | Excelから完全再構築 | 30件 |
| 4 | 【P】付きと無しの重複レシピ | 【P】無しを削除して統合 | 8件 |
| 5 | 通常レシピがis_intermediate=true | falseに修正しアイテム再登録 | 7件 |

### 19-4. 諸経費分類の一括修正

- **231件のmaterial→expense変更**
  - 総合水道光熱費: 108件
  - 内職: 68件（13円/10円/12円/28円の各種）
  - 【発送用ダンボール】: 54件
  - 発送件数: 1件

### 19-5. Amazon手数料のレシピアイテムからの完全削除

- **121件削除**: 従来各レシピにexpenseアイテムとして登録されていたAmazon手数料を全削除
- Amazon手数料は材料データベースの一括税率設定（販売価格×手数料率%、レシピごとON/OFFトグル）で管理する方針に統一

### 19-6. fetchIntermediateUsage クエリ最適化

- **問題:** レシピ一覧ページの`fetchIntermediateUsage`がSupabase行数制限(1000行)でエラー
- **修正:** 全recipe_itemsを取得するのではなく、`intermediate_recipe_id`でフィルタしてから取得するようにクエリロジックを最適化

### 19-7. レシピ詳細ページに原価小計行の追加

- 中間加工品セクション後に **「原価小計 (セット内容 + 原材料 + 中間加工品)」**（緑色、emerald-700）を表示
- 諸経費セクション後に **「原価小計 (資材・包材 + 諸経費)」**（オレンジ色、orange-700）を表示

### 19-8. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/recipe/[id]/page.tsx` | 小計行追加、fetchIntermediateUsage修正 |
| `app/recipe/page.tsx` | fetchIntermediateUsageクエリ最適化 |

---

## 20. JPG vs DB レシピ全数照合・データ修正 (2026-03-03)

### 20-1. 目的
Excel(PDF→JPG変換)の画像データとDBの全レシピを照合し、食材・資材・経費の不整合を修正する。

### 20-2. 実施した変更

#### 自社レシピ照合・修正（43件の変更、7レシピ）
- 蕎麦ふりかけ: 誤登録食材3件・資材4件を削除
- トマトドレッシング: 不足資材3件・経費1件を追加
- 喜多方ラーメンふりかけ: 不足資材1件・経費1件を追加
- CANP牛バラカレー・CANP豚角煮カレー: 不足資材・経費を追加
- 悪魔のBUTAカレー・ラッキーカレー: unit_price/cost値を修正

#### OEMレシピ照合・修正（29件の変更）
- OEM製造費 25件: `material`→`expense`に分類変更
- Re-Hcapキャップ 2件: `ingredient`→`material`に分類変更、単価修正

#### 自社レシピ経費一括修正（57件）
- 卸商品発送人件費 32件を全自社レシピから削除
- 内職13円→12円 25件を一括更新

#### レシピ管理画面UI整理
- 「データ統合」「Excelインポート」「重複チェック」の3ボタンを削除

---

## 21. KPIダッシュボード大規模改修 (2026-03-03〜04)

### 21-1. 目的
KPIダッシュボードの印刷レイアウトを完全再構築し、AI売上予測機能やサマリーカードの拡張、営業活動の前年度データ追加を行う。

### 21-2. 印刷レイアウトの再構築

#### 設計方針
- 画面表示は既存のまま維持し、印刷時のみ専用テーブルを表示
- A4横に収めるため12ヶ月を上期（8〜1月）・下期（2〜7月）に分割
- 部門別・営業活動・製造数の3テーブルを統一列幅で整列

#### 技術的な改善
- `rowSpan` による部門名セルの結合（5行分を1セルに統合）
- `table-fixed` + `w-[60px]` で全テーブルの列幅を完全統一
- `@media print` で画面用テーブル非表示 + 印刷用テーブル表示を切替

### 21-3. サマリーカードの拡張（4列→5列）

| カード | 内容 |
|--------|------|
| 年間売上合計 | 今年度実績・目標（既存） |
| **前年度売上合計（新規）** | 前年度合計・前年度対比%・前々年実績 |
| 進捗達成率 | 経過月ベース（既存） |
| 前年比 (YoY) | 前年同期比（既存） |
| **AI年度末売上予測（新規）** | Gemini 2.0 Flashによる年度末着地予想 |

### 21-4. AI年度末売上予測機能

#### 新規API: `/api/kpi/ai-forecast`
- **モデル:** Gemini 2.0 Flash（他APIと統一。商品説明生成のみ2.5 Flash使用）
- **入力:** 月次実績（前年・目標・実績）+ チャネル別売上データ
- **処理:** 実績確定月はそのまま使用、未到来月を季節性・トレンドから予測
- **出力:** `FORECAST: [金額]` 形式でパース
- **保存:** `localStorage` に保存（ページリロードしても保持、次ボタン押下で更新）

#### コスト比較（参考）
| 項目 | Gemini 2.0 Flash | Gemini 2.5 Flash |
|------|-------------------|-------------------|
| 入力 ($/100万トークン) | $0.10 | $0.30 |
| 出力 ($/100万トークン) | $0.40 | $2.50 |
| 1回あたり (約2,500トークン) | ≈0.06円 | ≈0.28円 |

### 21-5. 営業活動実績の前年度データ追加

#### データモデル変更
- `KpiSummary.salesActivity` に `lastYear: number` フィールドを追加
- 前年同月の `acquisition_actual` データを自動取得（`subYears` で1年前の月を参照）

#### DB投入（FY2025 営業活動実績）
| 8月 | 9月 | 10月 | 11月 | 12月 | 1月 | 2月 | 3月 | 4月 | 5月 | 6月 | 7月 |
|-----|-----|------|------|------|-----|-----|-----|-----|-----|-----|-----|
| 49 | 31 | 24 | 26 | 13 | 13 | 9 | 9 | 49 | 11 | 9 | 32 |

#### UI追加
- **前年度実績行:** テーブル最上段（グレー文字、件数表示）
- **前年度対比行:** テーブル最下段（パーセント表示、合計含む）

### 21-6. その他の修正
- **前年度対比の表示条件修正:** `r.actual > 0 && r.lastYear > 0` → `r.lastYear > 0`（前年データがあれば実績0でも0%表示）
- **テーブルはみ出し防止:** `overflow-x-auto` + `min-w-[900px]` で狭い画面でもスクロール対応

### 21-7. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/kpi/actions.ts` | salesActivity に lastYear 追加、前年同月データ取得ロジック |
| `components/kpi/KpiPageClient.tsx` | 印刷テーブル再構築、サマリー5列化、AI予測カード、営業前年度行 |
| `app/api/kpi/ai-forecast/route.ts` | Gemini 2.0 Flash 年度末売上予測API（新規） |
| `app/globals.css` | 印刷用スタイル（print:hidden / print:block 切替） |

---

## 22. WEB/卸販売 単価スナップショット改修・BASE CSVインポート修正 (2026-03-04~05)

### 22-1. 目的
商品価格変更時に過去の売上データが最新価格で再計算される問題を解消し、BASEのCSVインポートでの金額不一致・マッチング問題を修正する。

### 22-2. WEB販売管理 — 単価スナップショット

#### 問題
商品マスターの価格を変更すると、過去月の売上金額も最新価格で再計算されてしまい、過去データの正確性が失われる。

#### 修正
- `web_sales_summary` に `unit_price` カラム追加（インポート時の単価を保存）
- `calculateTotalAllECSites()` のフォールバック: `unit_price → price → productMap.price`
- 過去データは過去の単価で、新規データは現在の単価で正確に計算

### 22-3. 卸販売管理 — 同等修正
- `wholesale_summary` にも `unit_price` カラムを追加
- WEB販売と同じスナップショット方式を適用

### 22-4. OEM売上入力画面 — マスターデータ優先方式
- **問題:** 単価入力フィールドがあるが、結局マスターデータで上書きされて分かりにくい
- **修正:** 単価フィールドをマスターから自動取得・読み取り専用に変更。「単価変更はOEM商品管理から」と注記
- OEMエリアサマリーは `sale.unit_price`（実売上データ）を表示

### 22-5. BASE CSVインポート修正

#### 合計金額の取得（base-parse ver.5）
- CSVの「合計金額」列（23列目）もパースし、商品ごとに金額を集計
- `base_amount` として `web_sales_summary` に保存（参考データ用）

#### BASEのみ複数マッチ許可（csvHelpers.ts）
- **問題:** BASEは「送料別」「送料込み」で同一商品を2つ販売するが、一方がマッチすると他方が宙ぶらりんになる
- **修正:** `channel === 'base'` の場合のみ `matchedIds` 重複排除をスキップ
- 他ECサイトは従来通り1:1マッチを維持

#### 金額表示の設計方針
- クーポン・割引は広告費として扱い、売上金額には影響させない
- 全ECサイト共通: `count × マスター価格 = 本来売り上がった金額` を表示
- `get_monthly_financial_summary` RPC関数も `count × products.price` に統一

### 22-6. DBスキーマ変更
| テーブル | カラム | 型 | 用途 |
|---------|--------|---|------|
| `web_sales_summary` | `unit_price` | numeric | 単価スナップショット |
| `web_sales_summary` | `base_amount` | integer | BASE CSV実売金額（参考データ） |

### 22-7. 関連ファイル
| ファイル | 変更内容 |
|---------|---------|
| `app/api/import/base-parse/route.ts` | CSV合計金額列のパース・集計 |
| `app/api/import/base-confirm/route.ts` | base_amount保存、unit_price非上書き |
| `lib/csvHelpers.ts` | BASEのみ重複マッチ許可（allowDuplicate） |
| `components/BaseCsvImportModal.tsx` | confirm送信時にamountを含める |
| `utils/webSalesUtils.tsx` | unit_priceフォールバック対応 |
| `app/wholesale/oem-sales/page.tsx` | OEM単価フィールドを読み取り専用に |
| `components/wholesale/oem-area.tsx` | サマリー単価を実売データから表示 |
| DB RPC `get_monthly_financial_summary` | count x master price に統一 |



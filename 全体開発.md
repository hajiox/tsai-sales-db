# 全体開発メモ（現行版）

## 最終更新
2026-08-16

## 2026-08-16 DocScanner取引条件とTSAスマホ納品書の連動
- DocScannerを物販取引先の取引条件マスターとし、TSAのスマホ納品書はTSA側へ同期された `transaction_type` / `default_rate` を参照する。
- TSA画面と作成APIの両方でマスター値を優先する。DocScannerで条件を変更した際は専用同期APIで即時反映し、納品書同期時にも再同期する。
- 「道の駅あいづ湯川・会津坂下」は委託・7掛で本番確認済み。過去の発行済み納品書は自動変更しない。

## 正本
- 正本: `C:\作業用\tsai-sales-db\全体開発.md`
- Google Drive同期: `G:\マイドライブ\md\全体開発.md`
- 旧ファイル `C:\作業用\全体開発.md` は内容統合後に削除済み。

## 共通方針
- 既存業務を止めない。大きな改修でも既存導線を残し、段階的に追加する。
- 金額計算は原則「1円未満切り捨て」。卸価格シミュレーション、見積書、納品書、今後の請求書で統一する。
- 6.5掛/7掛は、税抜単価の端数を切り捨てた後に掛率を適用し、最後に消費税を計算する。
- 消費税の端数処理は、明細単位・伝票単位・月締め単位で結果が変わるため、請求書発行時に取引先運用へ合わせて明示する。
- APIキー、OAuth secret、アプリ秘密情報はMarkdownへ記録しない。
- TSA / TSG / DocScannerの各作業が完了したら、該当ローカルMDへ日付・要点・確認結果を軽く追記する。秘密情報は書かない。

## システム一覧

| システム | 場所 | 主なURL/起動 | 状態 |
|---|---|---|---|
| TSA | `C:\作業用\tsai-sales-db` | 本番 `https://v0-tsa-19.vercel.app` | レシピ、卸販売、スマホ納品書入口 |
| DocScanner | `C:\作業用\doc-scanner` | `http://localhost:3004` / LAN `http://192.168.110.200:3004` / PM2 `doc-scanner` | 書類管理、FAX、見積、物販納品書、EC速報 |
| TSG | `C:\作業用\ts-groupware` | 本番 `https://v0-line-blush.vercel.app` | 社内グループウェア、通知、PWA |
| Yamato Analytics | `C:\作業用\yamato-analytics` | `http://localhost:3003` / LAN `http://192.168.110.200:3003` / PM2 `yamato-analytics` | 出荷分析、配送区分・運賃平均 |
| Excel Analyzer | `C:\作業用\excel-analyzer` | 必要時起動 | Excel解析 |
| DisGrav | `C:\作業用\disgrav` | 必要時起動 | Antigravity/Discord遠隔操作実験 |

## 現行の重要連携

### TSA -> Yamato Analytics
- TSAサイドバーの「伝票発行システム」直下から、社内LANのヤマト出荷データ管理へ遷移する。
- ヤマト側の配送サマリーは期間フィルタと連動し、EC別配送区分件数に加えて全国・地域別の平均運賃概算を表示する。
- 運賃は2026-06-23付ヤマト運輸見積書の税抜料金を基準とする。元CSVに荷物サイズがないため、見積書の地域別サイズ構成と実配送先件数で加重平均し、実請求額とは区別する。

### DocScanner 物販納品書 -> TSA卸販売
- DocScannerで物販納品書を発行すると、TSA `POST /api/wholesale/delivery-note-sync` へ明細を送る。
- TSA側は `wholesale_delivery_note_sales` に原本明細を保存し、`wholesale_sales` へ商品別・事業者別に集約する。
- 既存の手入力は残す。手入力は自動集計との差分補正として扱う。
- 商品紐付けは `delivery_note_items.source_recipe_id` -> `recipes.linked_wholesale_product_id` を優先し、次に商品名正規化で照合する。
- 取引先はDocScanner取引先IDまたは正規化名称でTSA卸先に紐付ける。

### TSA WEB発行納品書 -> DocScanner
- TSA本番のWEB発行納品書は、DocScanner側で取得してローカルDBへ同期する。
- DocScannerの物販納品書一覧・書類一覧から印刷プレビューできる。
- 同期は一覧表示ごとに過剰アクセスしないようキャッシュする。

### 商品マスター
- 物販納品書・スマホ納品書の商品選択はTSAレシピシステムを正とする。
- 基本は `自社` タブ、必要に応じて `OEM` タブも使用する。
- レシピコピー時にWEB販売・卸販売などの紐付けを自動コピーしない。コピー先で意図しない紐付けを持つ事故を防ぐ。

## DocScanner 現行メモ
- PC版物販納品書は、発行先企業カラム、企業別履歴、過去納品書検索、日/月/年切替、一括削除・一括ステータス変更に対応。
- 納品書は金額を印字しない運用が多いため、商品単価印字はチェック式。デフォルトは印字しない。
- スマホ用58mm納品書はTSA側の単独画面として運用。金額、取引形態、掛率メモ、履歴、削除、ステータス変更は出さない。
- スマホ印刷は Star PassPRNT 経由でスター精密 SM-S210i に印刷する。OS標準プリンター一覧には出ない前提。
- 見積書は物販納品書と同じ一覧UIへ寄せる。備考にはTSAレシピの `販売価格 (Selling Price)` 税抜値を希望小売価格として出せるようにする。
- 営業FAX/見積送信は、取引先企業を左に出し、FAX/メールの両方送信も許容する。
- eFAX取り込みは原稿が上下逆の場合に自動補正する。
- EC速報は通知量が多いためブラウザ通知なし。FAX受信・新着書類など必要な通知だけ残す。

## TSA 現行メモ
- Googleログインは `prompt=select_account` を指定し、別GoogleアカウントでChromeログイン済みでもアカウント選択を出す。
- 権限外アカウントでAccessDeniedになった場合は `/login` に戻し、権限のあるアカウントを選び直す案内を出す。
- レシピの充填量/内容量は完全フリーテキスト。`g` / `ml` / `自由` の単位選択は廃止。
- TSA本番は `https://v0-tsa-19.vercel.app`。

## TSG 現行メモ
- TSGはLINEログイン/PWA前提。iOS通知はSafariでホーム画面追加したPWAのみ実用対象。
- 投稿者本人には本人投稿の通知を送らない。掲示板、グループChat、DMは通知生成対象だが、実際の受信は端末ごとのWeb Push購読・ミュート設定で決まる。
- DMとグループ未読は下部ナビとPWA App Badgeへ反映する。
- 管理画面のグループ一覧は、掲示板を青、グループChatを緑で色分けする。
- DocScanner/TSAと同じ会津ブランド館系のファビコン・OGPを設定済み。

## TikTok Shop API
- Partner Center上のサービス `会津ブランド館` はドラフト状態。
- プロフィール/カテゴリー申請は審査中。
- データセキュリティおよびプライバシー審査はアンケート送信済み。
- API管理では `Order Information` と `Shop Authorized Information` が送信待ち。まだ利用許可確定ではない。
- サービスID: `7639211980491392776`
- アプリ秘密情報はMarkdownへ記録しない。

## 直近の確認
- TSA: `npm run build` 成功、本番デプロイ済み。
- TSG: `npm run build` 成功。ローカル `npm run lint` は `eslint` コマンド未解決。
- DocScanner: PM2 `doc-scanner` でローカル常駐運用。

## 削ったもの
- 古い日別作業ログ、試行錯誤ログ、重複した説明、完了済みの細かいUI修正履歴は削除。
- 必要な場合はGit履歴またはGoogle Driveの版履歴から確認する。

## 2026-06-02 DocScanner 取引先グループ整理
- 取引先管理のAI判定ラベルは表示しない方針へ変更。DB上の `counterparty_roles` / `counterparties.type` は保持し、手動グループ運用を主軸にする。
- `物販納品先` グループを作成し、`物販取引先リスト.xlsx` の68件を既存取引先に紐付けた。未一致0件、新規取引先作成なし。
- DocScanner物販納品書発行の発行先は `物販納品先` グループのみから取得する。`/api/counterparties?groupName=物販納品先` で取得。
- 確認: DocScanner `npm run build` 成功、PM2 `doc-scanner` 再起動済み。

## 2026-06-03 Supabase RLS Security Advisor対応
- Supabase project `tsai-sales-db` (`zrerpexdsaxqztqqrwwv`) の `rls_disabled_in_public` 警告を確認。
- 対象は `app_data_migrations`, `recipes_selling_price_tax_included_backup_20260531`, `trial_balance_uploads`, `trial_balance_accounts` の4テーブル。
- `supabase/migrations/20260603_secure_public_rls_advisor_tables.sql` を追加し、本番DBへ適用済み。
- 4テーブルはRLSを有効化し、`anon` / `authenticated` の直接権限を剥奪。`service_role` は維持。
- 試算表系はブラウザ直アクセスではなく `/api/finance/trial-balance-statement` 経由のDB接続で使うため、既存画面影響なし。
- 確認: publicテーブルのRLS無効数0、対象4テーブルの匿名SELECTは権限エラー、ローカル試算表APIはHTTP 200。
- `postgres` 所有の将来オブジェクトは匿名/認証済みへのデフォルト公開を停止。`supabase_admin` 側の既定権限はDB接続ユーザーから変更不可のため、今後その所有者で作るテーブルは作成SQL内で明示的にRLS/権限を設定する。
## 2026-06-03 Supabase secret cleanup / rollback準備
- 2026-06-03 RLS対応の非常用ロールバックSQLを `sql/20260603_rollback_secure_public_rls_advisor_tables.sql` に追加。
- 古い手動スクリプト内に残っていたSupabase URL / service role JWT / DB接続文字列の直書きを削除し、`.env.local` / `.env` の `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL` 参照へ変更。
- 検証: `.env*` 以外に対象機密文字列が残っていないことを確認。変更TSスクリプトの構文診断0、変更JS/MJSの `node --check` 成功、`npm run build` 成功。
- Supabaseキー本体のローテーションは未実施。理由: この環境にSupabase CLI / 管理トークンがなく、新キー発行からVercel・ローカル・DocScanner連携env更新までを一括で安全に行えないため。実施時は新旧キー併用不可に注意し、Vercel/ローカル/PM2対象envを更新してから旧キー失効する。

## 2026-06-05 横断更新
- TSGは `https://v0-line-blush.vercel.app` へ最新デプロイ済み。Androidでスクロールできない報告に対し、ページ本文を通常スクロールへ戻し、Pull-to-refreshが通常スクロールを横取りしないよう修正。下部ナビ/投稿欄/Chat入力欄のスマホ固定も安定化した。
- TSGはタスク依頼、メンション、画像貼り付け、検索、Chat/DM編集削除、フォントサイズ2段階、スマホ下部メニュー/PWA周りを継続整備。PC版は中央幅、スマホ版は横流れ防止を優先する。
- DocScannerは eFAX受信PDFをJPEG化してTSG「FAX受信」掲示板へ投稿する連携を修正。PDF変換をBuffer処理に寄せ、今日スキップされていた3通を画像付きで復旧投稿済み。社内ファイルリンクは出さず、TSG上で画像確認できる形にする。
- DocScannerのEC速報はGmail注文メールを集計し、TSG「EC速報」へ定時投稿する。14時締め投稿は0件扱いではなく、その日の最終データとして投稿する方針。
- TSAはレシピ販売価格を税別ベースへ統一。税込からの入力は廃止方針。小数点以下は完全切り捨て、四捨五入しない。
- TSA財務分析は総勘定元帳に加えて試算表インポートを扱う方向。差異表示だけではなく、ダッシュボード上で解説・アラート・見るべきポイントを出す。
- TSA WEB管理/広告管理にAmazonタイムセール設定機能を追加。Amazon提供Excelを取り込み、画面編集、仮保存、履歴、Amazon形式出力を行う。
- DocScannerの営業FAX/一斉配信は、添付・プレビュー・件名本文・最終確認を必須にする。送信履歴は営業FAX側にも残し、FAX送信分はFAX送信一覧にも必ず入れる。
- Google Drive開発MD同期は、ユーザーOAuth refresh token方式からサービスアカウント方式へ切替。`sync_md_to_drive.js` はDrive同期先フォルダを現行MDフォルダへ修正し、既存4ファイルを上書き更新できることを確認済み。Google Auth Platformは `aizubrandhall@gmail.com` で `tsai-460605` の公開ステータスが本番環境であることを確認。サービスアカウントキーも新規発行キーへ差し替え済み。旧キーはロールバック用に保持。

## 2026-06-06 横断ルール更新
- 一作業完了ごとにローカルMDを軽く更新する運用へ変更。対象は主に `TSA開発.md`、`TSG開発.md`、`DocScanner開発.md`、必要に応じてこの `全体開発.md`。
- DocScanner営業FAXの宛先は取引先管理の `物販納品先` グループと連動。営業FAX側で外す場合も取引先マスター削除ではなく、グループ所属解除として扱う。

## 2026-07-15 DocScanner・TSG履歴書連携
- DocScannerで履歴書/職務経歴書を専用種別へ分類し、取引先には登録せず `取込済/履歴書` へ保管する。書類詳細からTSG人事管理へ送信でき、画像/PDFを4MB以下のPDFへ整えてsecret認証で連携する。
- TSGは履歴書PDFを非公開Storageへ保存し、既存のGemini OCRで人事情報の空欄を補完する。氏名一意一致は既存社員へ紐付け、同名複数は409で止め、原本ハッシュによる再送は重複登録しない。既存社員1件でPDF保存・OCR・冪等再送まで本番確認済み。

## 2026-07-26 物販採用商品・卸価格連携
- DocScannerに物販納品先ごとの採用商品・税抜卸価格・適用期間・根拠書類マスターを追加。書類調査で72社中31社の225商品を価格確認済みとし、根拠がない41社は従来のTSAレシピ価格と取引先掛率を継続する。
- 新規納品書は納品日時点の確認済み価格だけを使用し、要確認・概算商品は従来ロジックへ戻す。既存納品書の保存済み単価は変更しない。
- TSA卸販売管理の納品書同期APIは既存どおりDocScannerから受け取った `unitPrice` / `amount` を優先して保存するため、TSA側の機能変更なしで確定卸価格が連動する。価格ソースと根拠IDは同期元payloadにも保持する。
- 次工程の請求書発行では、現在の価格マスターで過去分を再計算せず、納品書明細に保存された単価スナップショットと納品数を集計する。請求書発行機能自体は今回未実装。

## 2026-07-26 物販価格調査の再集計

- 物販納品書の発行先を母集団にし、弊社発行請求書と先方の支払精算書を企業・店舗別に照合する方式へ修正。別名、連絡先、OCR宛名、精算書内の店舗見出しを使い、複数店舗の価格を混在させない。
- 再集計では72社中46社・353商品を根拠付き確定価格、11商品を要確認、94商品をレシピ価格フォールバックとした。初回集計の31社・225商品から確定範囲が増加した。
- TSA卸販売管理と将来の請求書発行はDocScanner納品書に保存された確定価格スナップショットを優先する。根拠なし・競合・商品一致が弱いデータは自動確定せず、従来のレシピ価格と掛率を使用する。

## 2026-08-06 ヤマト・佐川月次出荷データ
- ログイン済み各社画面から2026年7月CSVを取得し、ヤマト共有フォルダへ8分割6,082件、佐川共有フォルダへ234行を保存。佐川の削除区分2の2行は取込対象外とし、232件を登録した。
- ローカル集計システムを配送会社横断へ拡張し、同一注文の複数個口も送り状単位で保持する。顧客原本はPC・社内共有だけで処理し、Vercelへ送らない。
- 毎月の反復作業は `C:\Users\ts\.codex\skills\tsa-carrier-shipment-csv` の検証・取込スクリプトを使い、長期開発チャットを再利用しない。

## 2026-08-09 PCバックアップ・セキュリティ監査
- 主要4システム、SQLite、Git、Google Drive/OneDrive/NAS、Windows Defender/Firewall、LAN公開API、秘密情報、依存パッケージを読み取り監査した。設定変更は行っていない。
- 主要DB・原本・最新未コミットコードの独立した定期バックアップはなく、同一Cドライブ上の改修前手動DBコピーが中心。DocScanner/ヤマトの現行DBと直近手動DBコピーは `quick_check=ok`。
- TSA本番の未認証 `api/ai-tools` がパスワード項目を返すこと、DocScanner/ヤマトの未認証LAN API、Node.jsの過大なFirewall許可、秘密鍵/.envの広いACLを最優先課題として確認した。
- 詳細、RPO/RTO、3-2-1-1-0構成、段階導入順、復旧手順は `C:\作業用\PCバックアップ・セキュリティ監査_2026-08-09.md` に記録した。BitLocker、Supabase実プラン、NAS側世代管理は権限・管理画面の都合で未確認。

## 2026-08-11 PCバックアップ・早期復旧

- 業務PCのバックアップをNAS主系、Google Drive副系、復旧USBの3層に整理した。日次データは暗号化、週次はWindowsシステムイメージ、06:30に自動検証する。
- 初回完全バックアップと暗号化復元試験は成功。DocScanner/ヤマトSQLite、TSAクラウドDB、ローカル4システム、添付データ、Codex設定・Skills・状態DBを対象とする。Codexセッション全体は日曜バックアップとPCイメージで保持する。
- TSAの「バックアップ管理」に実行履歴、容量、DB検証、保存先、復旧手順を表示する。バックアップ処理からの履歴登録は既存TSA Bridge認証を共用し、秘密情報を追加していない。
- 定期実行は既存の管理者権限付きDocScanner起動タスクからPM2管理の専用スケジューラを復元する。ログイン前でも継続し、実行結果は毎回TSAの履歴へ残す。2026-08-11に無人経路の日次実行成功まで確認した。
- 監査履歴の再送は部分更新とし、NAS/Google Driveの保存先や元のバックアップ詳細が消えないよう修正した。本番TSAで最新世代の保存先・容量・DB検査を確認済み。
- NAS: `\\tshdd\disk\NEW\TSA-PC-Backup` / Drive: `G:\マイドライブ\TSA-PC-Backup` / USB: `D:\TSA-PC-RECOVERY`。
- 復旧はUSBの`README_最初にお読みください.html`を起点とし、データ復元はまず`C:\TSA-Restore-Staging`へ展開する。本番フォルダを自動上書きしない。

## 2026-08-19 TSAランタイム復旧監査

- DocScanner・TSAからTSGへの連携シークレット差異を解消し、秘密値を表示せずproductionへ同期するスクリプトを追加した。TSA本番から対象グループの投稿者10名を取得できる。
- 売上ダッシュボードの日付hydration不整合とSupabaseブラウザクライアント二重生成を修正し、Next.js、next-auth、Puppeteerを修正版へ更新した。ビルド187ルート、RLS・秘密情報検査、主要回帰テストに成功。
- PR #188、本番 `dpl_Fqw3s2kWvX4NsbiCpEV73D1obV6z`。主要8画面をログイン済みブラウザで確認し、表示・コンソールの致命的エラー0件。npm修正版のない `xlsx` の既知警告のみ継続監視する。

## 2026-08-20 レシピ・EC価格変更履歴

- レシピ詳細の販売価格欄に「前回変更前価格」と旧価格→新価格の変更履歴（新しい順・最大50件）を追加した。
- EC価格反映欄に、Amazon・楽天・Yahoo・メルカリ・BASE・Qoo10・TikTok別の前回変更前価格、変更日時、旧価格→新価格、商品識別子、反映結果を表示する履歴を追加した。既存のCodex Bridgeジョブに保存済みの検証済み価格計画を使用し、未検証ジョブは履歴へ混在させない。
- TSG通知は今回変更分の旧価格→新価格のみを冪等投稿する既存方式を維持し、履歴全件はTSA内だけで参照する。TSG本体をTSAリポジトリからデプロイしない。
- 確認: 販売価格・EC価格計画の回帰テスト成功、変更対象Lintエラー0、Next.js本番ビルド187ルート成功。リポジトリ全体の型検査には今回と無関係な既存エラーが残る。

## 2026-08-20 EC価格変更の進捗表示・Amazon画面追従

- Amazon Seller Centralの「新機能 / 出品、FBA、価格設定ツールのリンクが移動しました」は認証待ちではなく案内モーダルとして扱い、右上の閉じるボタンから商品一覧へ戻る手順を価格変更Skillへ追加した。Skill全体の再作成は行わず、Amazon手順だけを追補した。
- 実行中だった価格変更ジョブはユーザー指示で書込前に停止し、EC各社の価格が未変更であることを結果から確認した。停止履歴は「書込前に停止しました（価格変更なし）」へ修正した。
- レシピ詳細のEC価格反映欄に、変更前確認/反映中の2工程、進捗率、経過時間、最終応答、2分以上進捗が変わらない場合の停止疑いを表示するインジケーターを追加した。Bridge 1.8.10へ更新し、計画段階の失敗時は巨大な内部警告ではなく「価格変更なし」を明示する。
- 確認: 対象Lint、Bridge構文検査、販売価格・EC価格計画回帰テスト、Next.js本番ビルド187ルート、security scan成功。本番 `dpl_6pWdnKDtpkeHv2yjvptEd7FbhhRw` を `v0-tsa-19.vercel.app` へ割り当て、Bridge 1.8.10のheartbeatを確認した。
- 停止・完了後に進捗枠が消える問題を追加修正。APIが対象レシピの直近ジョブも返し、EC価格反映欄へ「直近の実行状況」を常設する。実行中は工程・進捗・最終応答、終了後は完了/停止/操作待ち/失敗と日時・結果を同じ場所に保持する。本番 `dpl_A9nYucTw6wRbsYzDVLjUaiMGwvYt` へ反映し、対象Lint、EC価格計画テスト、187ルートの本番ビルドに成功した。
- 同時刻に同じworker IDを使う別PCが価格ジョブを取得し、片方だけChrome Native Host欠落と誤判定した事象を確認。現行PCのNative Host登録・実体・Chrome接続は正常であり、該当履歴を「重複実行を停止・価格変更なし」へ訂正した。Bridge 1.8.11で旧版workerを拒否し、実行中workerへの二重割当を防止。価格Skillは1サイトずつ狭い範囲だけを読み、全画面DOMや全サイト同時操作を禁止した。本番 `dpl_9Be46zE748P8R3hvKukyHkiADGoc`、Bridge 1.8.11 heartbeat、旧版1.8.10のHTTP 409拒否を確認した。
- 2026-08-20: EC価格改定がログイン済みChromeタブを再利用せず新規タブを作り、BASE等を再ログイン扱いにした原因を修正。実行中ジョブをEC書込前に停止し、新規タブで誤判定した直前ジョブとともに `価格変更なし` で取消。Bridge/Skillの計画・書込フェーズを既存ログイン済みタブ専用とし、新規タブ・新規ウィンドウ・一時プロファイル・既存タブ終了を禁止した。対象タブが無い場合のみ、必要サイトを明記して操作待ちにする。Bridge 1.8.12。
- 2026-08-20: 前ジョブの強制停止でChromeのタブ利用権が残り、次ジョブが最新のロック中タブだけを選んで操作待ちになった事象を修正。Native Host再起動で残留ロックを解除し、同一ECの既存タブが複数ある場合は利用可能な候補を全て試してから操作待ちへ移る。計画フェーズの規約違反は強制終了せず正常終了させてタブを解放し、書込フェーズのみ安全停止する。Bridge 1.8.13。
- TSA画面からCodexを呼ぶ全機能は、巨大Chat・開発task履歴を渡さず、専用Skillと最小限の構造化データだけを新規ephemeralセッションへ渡す。専用Skillがない反復処理は、Skill作成・検証前にアプリ実行ボタンを有効化しない。
- EC価格ジョブが停止・操作待ち・確認待ち・失敗になった場合、直近状況欄へ「同じ対象で再実行」を表示する。取消済みジョブでは無効化前のサイト別エラーを再掲せず、停止理由と価格未変更だけを示す。本番 `dpl_68E65G5QwkS3nCW3SdoDzdMBVQDr`、対象Lint、EC価格計画テスト、187ルートの本番ビルド成功を確認した。
## 2026-08-21 EC価格変更の商品LP必須連動

- レシピに `product_lp_url` が登録されている場合、EC価格変更ジョブへLP URLを固定snapshotとして渡し、該当LPの公開価格確認までを必須工程にした。
- LP編集元は既存ローカルクローンを使わず、Vercel接続先GitHubの本番ブランチをジョブごとに新規クローンして特定する。計画時に商品・ファイル・旧価格・新価格を確定できなければEC書込前に停止する。
- LPは計画箇所だけ変更し、build、GitHub push、Vercel公開、登録済みLP URLの実価格を確認する。LP未反映ならジョブを完了にしない。EC・LPの変更前後価格と公開コミットを履歴表示する。
- GitHubを最新版の正とし、TSA開発も毎回新規クローンから開始するルールを `AGENTS.md` に追加した。

## 2026-08-25 裏ラベルチェックTSA移植

- DocScannerの通常・簡易チェックをTSAへ移植し、PCサイドバーの「バックアップ管理」直下とスマホトップへ追加した。主利用は簡易チェックとし、撮影後すぐ軽量OCRを開始する。
- 簡易は画像1枚・日付項目限定・レシピ照合なし、通常は最大4画像・原材料OCR・TSAレシピ照合を維持する。履歴画像は非公開Supabase Storageへ保存する。
- 旧SQLiteから履歴169件・画像146件を欠損0で移行し、方式・判定内訳も一致した。旧DocScannerデータは復旧用に保持し、新規運用の正本はTSAとする。
- 検証: 対象Lintエラー0、本番相当ビルド189ルート、未認証拒否、PC/スマホ表示、画像配信、保存冪等性に成功。簡易OCRは旧画像3枚で3/3一致、約1.4〜2.2秒。
- 詳細は `TSA_LABEL_CHECK_NOTE.md` を参照する。
